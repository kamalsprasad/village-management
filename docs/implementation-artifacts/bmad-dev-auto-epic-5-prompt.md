# /bmad-dev-auto Prompt — Epic 5 Final Story (5.10 Sub-Stories: 5.10a → 5.10e, with 5.10e split into 5.10e1 → 5.10e4)

> **Usage:** Copy everything below the `---` line into a fresh `/bmad-dev-auto` invocation.
> **Adapting for subsequent iterations:** Change ONLY the three sections marked
> `<<< CHANGE PER ITERATION >>>` — (1) Current Iteration Target, (2) Story X.Y Specifics,
> (3) Review invariants. Everything else is campaign-level context that stays constant.
> The MVP dependency order in "Epic 5 MVP Scope" tells you which sub-story is next after
> each completion.
>
> **Sub-story split (user decision 2026-08-04):** Story 5.10 "System Completion" has 9 broad
> ACs spanning 6+ independently shippable features. Per the bmad-dev-auto discipline rules
> ("HALT if the intent resolves to multiple independently shippable goals"), 5.10 is split
> into sub-stories 5.10a–5.10e, each processed in its own iteration. AC8 (System Health
> Monitoring) is deferred to post-MVP with thorough documentation (user decision 2026-08-04).
> AC3 (Notifications) is included in MVP as sub-story 5.10c (user decision 2026-08-04).
>
> **5.10e sub-split (user decision 2026-08-11):** 5.10e itself covers 4 broad ACs
> (AC4 UX polish/accessibility, AC5 performance, AC6 mobile responsiveness, AC9 final
> testing checklist) — the same multi-goal pattern that caused 5.10 to be split. Per the
> bmad-dev-auto discipline, 5.10e is pre-split into sub-stories 5.10e1–5.10e4, each
> processed in its own iteration. AC9's automated test infrastructure is deferred to a new
> post-MVP testing epic (user decision 2026-08-11); 5.10e4 produces only a manual final
> testing checklist doc for MVP. Completing 5.10e4 marks 5.10 and Epic 5 as done.

---

You are running bmad-dev-auto for the Sustainable Model Village Management System (village-app). This invocation processes EXACTLY ONE story this iteration and HALT cleanly so the next iteration can pick up the next story.

## Current Iteration Target <<< CHANGE PER ITERATION >>>

Epic: 5 — "Village Calendar, Storage, Optional Modules, and User Management"
Story to implement THIS iteration: 5.10e3 — Mobile Responsiveness (AC6)
Epic context file to load/compile: {implementation_artifacts}/epic-5-context.md
Spec file to produce: {implementation_artifacts}/spec-5-10e3-mobile-responsiveness.md

If the spec file already exists with status `draft`, resume it. If it exists with any other status, do NOT overwrite — HALT with blocking condition `spec already in progress/done; user decision required`.

## Epic 5 MVP Scope (do NOT implement deferred stories)

MVP stories, in dependency order (stories marked ✅ are done; ← THIS ITERATION marks the current target):

1. ✅ 5.1 Village Calendar - Global Calendar with Category Filtering (deps: Epic 1) — DONE 2026-07-28
2. ✅ 5.2 Village Calendar - Role-Based Event Creation and Editing (deps: 5.1) — DONE 2026-07-29
3. ✅ 5.3 Cloud Storage - Role-Based Storage Quotas and Personal Folders (deps: 1.10) — DONE 2026-07-30
4. ✅ 5.4 Cloud Storage - Shared Folders and Module-Based Access (deps: 5.3) — DONE 2026-07-31
5. ✅ 5.7 Vendors/Suppliers Management Module (deps: 2.2, 2.3, 3.8 — all done) — DONE 2026-08-01
6. ✅ 5.9 Module Management and Configuration (deps: all MVP previous) — DONE 2026-08-03
7. ✅ 5.14 Authentication Completeness - Password Change and Reset (deps: 1.3, 1.11) — DONE 2026-08-03
8. ✅ 5.12 User Management - CRUD Operations (deps: 1.4, 1.11) — DONE 2026-08-03
9. ✅ 5.13 Role Assignment and Permissions Management UI (deps: 5.12) — DONE 2026-08-04
10. ✅ 5.11 Start Fresh Production Setup Wizard (deps: 5.9, 5.12) — DONE 2026-08-04
11. 5.10 System Completion — split into sub-stories (user decision 2026-08-04; 5.10e re-split 2026-08-11):
    - ✅ 5.10a Dashboard Completion — Real Data Wiring and Widget Finalization (AC1) — DONE 2026-08-04
    - ✅ 5.10b Navigation Polish — Breadcrumbs and Quick Search (AC2) — DONE 2026-08-05 (post-finalization patch `3152db1` applied; see spec-5-10b Review Triage Log §2026-08-05)
    - ✅ 5.10c Notifications System (AC3) — includes new Appwrite `notifications` table — DONE 2026-08-05 (post-review patch `f5e5fc9` applied; see spec-5-10c Review Triage Log §2026-08-05)
    - ✅ 5.10d Help and Documentation (AC7) — DONE 2026-08-10 (post-review patch `4abd369` applied; see spec-5-10d Review Triage Log §2026-08-10)
    - 5.10e UX Polish, Performance, Mobile Responsiveness, and Final Testing Checklist — split into sub-stories (user decision 2026-08-11):
      - ✅ 5.10e1 UX Polish and Accessibility (AC4) — DONE 2026-08-11 (post-finalization patches `26779c6` page-header standardization + `6f251e7` page margins applied; see spec-5-10e1 Review Triage Log §2026-08-11)
      - ✅ 5.10e2 Performance Optimization (AC5) — DONE 2026-08-11 (post-review patch `fe553e9` full-page-reload-on-logout to clear cached Pinia store state applied; see spec-5-10e2 Review Triage Log §2026-08-11)
      - 5.10e3 Mobile Responsiveness (AC6) ← THIS ITERATION
      - 5.10e4 Final Testing Checklist (AC9 — manual doc; automated test infra deferred to new post-MVP testing epic)
    - AC8 System Health Monitoring — DEFERRED to post-MVP (user decision 2026-08-04; documented in deferred-work.md during 5.10a; 5.10e4 will verify/expand the deferral entry)

DEFERRED (post-MVP — out of scope, do NOT implement, do NOT add toggles for them): 5.5 Guests, 5.6 Equipment, 5.8 Energy, 4.9–4.11, 5.10 AC8 (System Health Monitoring).

## Story 5.10e3 Specifics <<< CHANGE PER ITERATION >>>

Intent: Deliver the Mobile Responsiveness sub-story for MVP Story 5.10. Implement Story 5.10 AC6: "Mobile responsiveness: all pages functional on mobile (320px+), touch-friendly (44px minimum)." This sub-story is an AUDIT + BOUNDED-FIX pass across the existing MVP frontend — it audits all 66 .vue page files for 320px-width breakage (dialogs with `min-width > 320px`, no-wrap flex rows, tables without horizontal-scroll containers, fixed-width assumptions), audits all icon-only/small buttons for 44px touch-target compliance, fixes the FarmDashboardPage clickable-card keyboard a11y (deferred from 5.10e1 per user decision 2026-08-11), and adds a GLOBAL CSS approach (in `src/css/app.scss`) to address the bulk of dialog-width and touch-target issues efficiently, supplemented by TARGETED per-file fixes for edge cases that global CSS cannot handle. This is a REUSE-ONLY story — NO new Appwrite tables, server functions, permissions, `.env` variables, stores, pages, or routes. It MODIFIES existing component templates, scoped styles, and `src/css/app.scss` (currently empty) for responsive behavior. This is Story 5.10 AC6 only — AC4 (UX polish/accessibility) was 5.10e1 (DONE), AC5 (performance) was 5.10e2 (DONE), AC9 (final testing checklist) is 5.10e4. AC8 (System Health Monitoring) is deferred to post-MVP.

Like 5.10e1 and 5.10e2, 5.10e3 is REUSE-ONLY: it adds NO new Appwrite tables, NO new server functions, NO new permissions, NO new `.env` variables, NO new stores, NO new pages, and NO new routes. It MODIFIES a bounded set of existing component templates, scoped `<style>` blocks, and the global `src/css/app.scss` for responsive behavior. It REUSES the existing Quasar grid system (`col-12`/`col-md-*`/`col-sm-*`), the existing `$q.screen` breakpoint utilities (`xs`/`sm`/`md`/`gt.xs`/`lt.sm`/`lt.md`), the existing Quasar breakpoint classes (`lt-md`/`gt-xs`), and the existing `@media (max-width: 599px)` pattern already used in 9 files (StoragePage, SharedStoragePage, DashboardPage, RecordSaleDialog, HarvestEntryDialog, SampleDataBanner, ProfilePage). No existing module server functions, Appwrite tables, permissions, or route definitions are touched.

ACs (from docs/epics.md Story 5.10 AC6 — treat as authoritative):

6. Mobile responsiveness: all pages functional on mobile (320px+), touch-friendly (44px minimum).

Sub-ACs for 5.10e3 (derived from AC6):

6a. **320px audit and global CSS fix.** Audit all 66 .vue page files for 320px-width breakage. The spec MUST grep for `min-width` in inline `style` attributes and scoped `<style>` blocks and enumerate every dialog/card with `min-width > 320px`. Known findings from the pre-derivation audit (the spec MUST verify these and add any others found): 20+ dialogs with `min-width` ranging from 340px to 600px (ResidentForm 500px, HouseholdForm 500px, TransactionForm 500px, RecordSaleDialog 600px, HarvestEntryDialog 600px, FinanceSettingsPage 500px, AddFundingDialog 450px, ClassDetailPage 450px, BellSchedulesSettingsPage 400px, VillageSettingsPage 400px, FinanceTransactionsPage 400px, EventFormDialog 400px, CalendarEventsSettingsPage 400px, TeachersListPage 400px, ClassesListPage 400px, AcademicTermsSettingsPage 380px, UpdateStatusDialog 380px, PlantingsListPage 340px, HouseholdsListPage 350px, LearnerAcademicsTab 400px). The GLOBAL fix: add a media query in `src/css/app.scss` that constrains all `q-dialog` cards on mobile (e.g., `@media (max-width: 599px) { .q-dialog__inner .q-card { max-width: 95vw !important; min-width: 0 !important; width: 95vw !important; } }` — the `!important` is needed to override inline `style` attributes). The spec MUST verify this global rule actually overrides the inline `min-width` styles (test by loading a dialog at 320px viewport). TARGETED per-file fixes: for dialogs where the global CSS is insufficient (e.g., the `RecordSaleDialog`/`HarvestEntryDialog` use a CSS class `.record-sale-dialog`/`.harvest-entry-dialog` with `min-width: 600px` in a scoped style block — the global rule may not penetrate scoped styles; the spec MUST fix these scoped styles with their own `@media (max-width: 599px)` override). CAP at 20-25 dialog fixes (global CSS + targeted scoped-style overrides).

6b. **44px touch-target audit and fix.** Audit all icon-only and small buttons for 44px touch-target compliance. Known findings from the pre-derivation audit (the spec MUST verify): 25+ `flat dense round` icon-only buttons across MainLayout (hamburger, notifications bell, help button), StoragePage (5 action buttons), school module (15+ navigation arrows and edit/delete buttons), farm module (widget refresh buttons, FarmAlertsPage dismiss `size="xs"`), and dashboard widgets. The GLOBAL fix: add a media query in `src/css/app.scss` that increases touch-target size for dense round buttons on touch devices/mobile (e.g., `@media (max-width: 599px) { .q-btn.dense.round { min-width: 44px; min-height: 44px; } }` — or use `@media (hover: none) and (pointer: coarse)` for touch-device targeting). The spec MUST verify this doesn't break desktop layouts (the rule only applies on mobile/touch). TARGETED per-file fixes: for `size="xs"` buttons (FarmAlertsPage dismiss, TimetableCellEditor clear) that are severely under-sized — remove `size="xs"` or add explicit `padding` overrides. For `q-chip` with `size="10px"` (TeachersListPage) — these are display-only chips, not touch targets; verify and exclude if non-interactive. CAP at 15-20 touch-target fixes (global CSS + targeted per-file).

6c. **No-wrap flex row fixes.** Fix the 3 known no-wrap flex rows that cause horizontal overflow at 320px: (1) `FinanceTransactionsPage.vue:164` (`class="row items-center no-wrap q-gutter-xs"`), (2) `TeachersListPage.vue:67` (`class="row items-center no-wrap"`), (3) `InventoryDetailPage.vue:351` (`class="row items-center no-wrap"`). The spec MUST either remove `no-wrap` (allow wrapping) or add a `@media (max-width: 599px)` override that allows wrapping. The spec MUST verify the layout still looks correct on desktop (no-wrap preserved on wider screens). CAP at 3-5 no-wrap fixes.

6d. **Table horizontal-scroll audit.** Audit the 35 files with `q-table` for 320px horizontal-scroll behavior. Quasar `q-table` has built-in horizontal scroll when columns exceed container width — verify this works at 320px. For `q-markup-table` (static HTML tables without q-table's scroll), wrap in a `<div style="overflow-x: auto">` if not already wrapped. The spec MUST enumerate any tables that overflow without a scroll container and fix them. CAP at 5-10 table-scroll fixes (most q-table instances should already scroll; only q-markup-table and custom table layouts need wrapping).

6e. **FarmDashboardPage clickable-card keyboard a11y (deferred from 5.10e1).** The clickable module-navigation cards in `src/modules/farm/pages/FarmDashboardPage.vue` (lines ~217-238) lack `role="button"`, `tabindex="0"`, `@keydown.enter`/`@keydown.space.prevent` handlers, and `:focus-visible` styling. 5.10e3 MUST add these (per user decision 2026-08-11, this item is folded into 5.10e3 since both the 44px touch-target audit and clickable-card keyboard a11y are interaction-surface concerns). The spec MUST add `role="button"`, `tabindex="0"`, `@keydown.enter="$router.push(link.route)"`, `@keydown.space.prevent="$router.push(link.route)"`, and a scoped `:focus-visible` CSS rule with a visible outline. This is a specific, enumerated fix — no cap needed.

6f. **MainLayout mobile header audit.** Verify the MainLayout header elements work at 320px: hamburger menu (`lt-md`, present), search bar (`gt-xs` — hidden on xs, verify this is acceptable or add a mobile search affordance), notifications bell, help button, user profile. The search input `style="width: 260px"` and search menu `style="min-width: 260px"` — verify these don't overflow at 320px (260px < 320px so should fit, but verify with the hamburger menu + other header elements all visible). The notification menu `style="width: 360px; max-width: 90vw"` — the `max-width: 90vw` handles 320px (288px). The spec MUST document the 320px header layout and flag any overflow. CAP at 3-5 header fixes if needed.

CAP RATIONALE: AC6 spans 320px dialog fixes, 44px touch-target fixes, no-wrap flex rows, table scroll, FarmDashboardPage keyboard a11y, and MainLayout header audit — each sub-AC (6a-6f) caps its fix set. The GLOBAL CSS approach (user decision 2026-08-11) in `src/css/app.scss` addresses the bulk of 6a (dialog min-width) and 6b (touch-target sizing) efficiently, reducing the per-file fix count. The spec MUST enumerate the exact fix set per sub-AC (file:line + fix description, or global CSS rule + justification) — do NOT leave the fix set as TBD. If the total fix count across all sub-ACs exceeds one iteration even with the global CSS approach, set `warnings: [multiple-goals]` and HALT with a proposal to split 5.10e3 into 5.10e3a (320px audit + global CSS + dialog fixes + no-wrap + tables — 6a/6c/6d/6f) and 5.10e3b (44px touch targets + FarmDashboardPage keyboard a11y — 6b/6e) — per the bmad-dev-auto discipline. Do NOT silently expand scope.

Prerequisites confirmed done: ALL prior MVP stories in all epics (1.1–1.11, 2.1–2.9, 3.1–3.10, 4.1–4.8, 4.12–4.13, 5.1–5.4, 5.7, 5.9, 5.11–5.14) are `done` in `docs/sprint-status.yaml`, and **5.10a, 5.10b, 5.10c, 5.10d, 5.10e1, and 5.10e2 are `done`** (5.10e2 done 2026-08-11 after post-review patch `fe553e9` full-page-reload-on-logout; see spec-5-10e2 Review Triage Log §2026-08-11). 5.10e3 depends on 5.10e2 — it audits the pages/components that 5.10a–5.10e2 built (including 5.10e1's page-header standardization across 65 pages, 5.10e2's dynamic chart.js imports, and the 5.10e2 caching guards). No spec file exists yet for 5.10e3.

**REUSE-ONLY (no new Appwrite infrastructure — user decision 2026-08-11):**

- NO new tables, NO new server functions, NO new permissions, NO new `.env` variables, NO new stores, NO new pages, NO new routes.
- MODIFIED frontend artifacts: `src/css/app.scss` (currently empty — gets the global responsive CSS rules), a bounded set of existing component templates (FarmDashboardPage keyboard a11y attributes, no-wrap flex row class changes), and a bounded set of scoped `<style>` blocks (dialog min-width media queries for scoped-style dialogs like RecordSaleDialog/HarvestEntryDialog). The spec enumerates exact files per sub-AC (6a-6f) with file:line + fix.
- NEW frontend artifacts: NONE (the global CSS goes in the existing `src/css/app.scss` file — no new files).
- NO backend changes whatsoever.

**Existing app structure to audit (do NOT break functionality):**

5.10e3 is a mobile-responsiveness-audit-and-fix pass across the existing MVP frontend. The app (after 5.10e2) renders via `MainLayout.vue` with a hamburger menu on mobile (`lt-md`), a drawer with `show-if-above`, route-level lazy-loading for all 24 routes, dynamic chart.js imports, and Pinia store caching on high-traffic stores. The Quasar grid system (`col-12`/`col-md-*`) is widely used (75 files) and provides a good responsive foundation. `$q.screen` is used in 7 files for conditional rendering. 9 files already have `@media (max-width: 599px)` queries. `src/css/app.scss` is currently empty (no global responsive CSS). `src/css/quasar.variables.scss` has no custom breakpoints (uses Quasar defaults: xs 0-599px, sm 600-1023px, md 1024-1439px). 5.10e3 MUST NOT break any page's functionality — it only changes CSS rules, adds keyboard a11y attributes, and adjusts flex-wrap behavior. The 5.10b quick-search (hidden on xs via `gt-xs`), 5.10c bell + badge + panel + realtime subscription, 5.10d help icon + menu + tooltips, 5.10e1 loading states + error handling + a11y fixes, 5.10e2 caching guards + dynamic imports, `expandedSections` watcher, and all module CRUD flows must continue to work after the mobile-responsiveness pass.

Continuity context from prior work (read these files before scaffolding):

- `src/css/app.scss` — currently contains only `// app global css in SCSS form`. 5.10e3 adds the global responsive CSS rules here (dialog min-width override on mobile, touch-target sizing for dense round buttons). Read before editing.
- `src/css/quasar.variables.scss` — contains color variables only, no breakpoints. 5.10e3 uses Quasar's default breakpoints (xs 0-599px). Read to confirm no custom breakpoint config exists.
- `src/layouts/MainLayout.vue` — the main layout with hamburger menu, drawer, header. 5.10e3 audits the header elements at 320px (search bar `gt-xs`, notification menu `width: 360px; max-width: 90vw`, help menu, user menu). Read the full header section (lines 1-260) and the handleLogout function (line ~1078, now using `window.location.href` per patch `fe553e9`).
- `src/modules/farm/pages/FarmDashboardPage.vue:217-238` — the clickable module-navigation cards. 5.10e3 adds `role="button"`, `tabindex="0"`, `@keydown.enter`/`@keydown.space.prevent`, and `:focus-visible` styling. Read the full card section and the scoped `<style>` block before editing.
- `src/modules/farm/components/RecordSaleDialog.vue:305-315` and `src/modules/farm/components/HarvestEntryDialog.vue:433-440` — these 2 dialogs use scoped CSS classes (`.record-sale-dialog`/`.harvest-entry-dialog`) with `min-width: 600px` and already have `@media (max-width: 600px)` queries — but 320px still breaks. 5.10e3 MUST verify whether the global `app.scss` rule penetrates these scoped styles (it may not — scoped styles add a `data-v-*` attribute selector that increases specificity). If the global rule doesn't penetrate, add a `@media (max-width: 599px)` override inside each scoped style block.
- `src/components/residents/ResidentForm.vue:2`, `src/components/households/HouseholdForm.vue:2`, `src/modules/finance/components/TransactionForm.vue:578`, `src/modules/finance/components/AddFundingDialog.vue:2` — dialogs with inline `style="min-width: 450-500px"`. The global `app.scss` rule with `!important` should override these inline styles. The spec MUST verify this works.
- `src/modules/finance/pages/FinanceTransactionsPage.vue:164` — `class="row items-center no-wrap q-gutter-xs"` — 5.10e3 removes `no-wrap` or adds a mobile wrapping override.
- `src/modules/school/pages/TeachersListPage.vue:67` — `class="row items-center no-wrap"` — same fix.
- `src/pages/inventory/InventoryDetailPage.vue:351` — `class="row items-center no-wrap"` — same fix.
- `docs/implementation-artifacts/spec-5-10e1-ux-polish-and-accessibility.md` — 5.10e1 spec (DONE); read for the page-header standardization (`26779c6` — `<h4 class="text-h5 q-my-none">` + `<p class="text-grey-7 q-mb-none">` subtitle across 65 pages) and the 5.10e1 a11y fixes (icon-only aria-labels, notification-badge live region). 5.10e3's touch-target fixes MUST NOT remove or conflict with 5.10e1's aria-labels. The FarmDashboardPage clickable-card keyboard a11y was deferred from 5.10e1 to 5.10e3 (see spec-5-10e1 deferred frontmatter).
- `docs/implementation-artifacts/spec-5-10e2-performance-optimization.md` — 5.10e2 spec (DONE); read for the dynamic chart.js import pattern (5.10e3's CSS changes must not break chart rendering) and the caching guards (5.10e3's mobile layout changes must not interfere with store fetch logic). NOTE the post-review patch `fe553e9` (full-page-reload-on-logout).
- `docs/implementation-artifacts/spec-5-10b-navigation-polish-breadcrumbs-and-quick-search.md` — 5.10b spec (DONE); read for the Breadcrumbs.vue responsive behavior (`$q.screen.gt.md`/`$q.screen.md` for breadcrumb display, mobile back-button-only) and the quick-search `gt-xs` hiding behavior. 5.10e3 MUST verify breadcrumbs still work at 320px and decide whether the quick-search hiding on xs is acceptable or needs a mobile search affordance.
- `docs/implementation-artifacts/spec-5-10c-notifications-system.md` — 5.10c spec (DONE); read for the notification bell + panel responsive behavior (`$q.screen.xs` vs `$q.screen.gt.xs` — q-dialog mobile, q-menu desktop). 5.10e3 MUST verify the notification panel works at 320px.
- `docs/implementation-artifacts/spec-5-10d-help-and-documentation.md` — 5.10d spec (DONE); read for the help menu responsive behavior (`$q.screen.xs` vs `$q.screen.gt.xs`). 5.10e3 MUST verify the help menu works at 320px.
- `docs/implementation-artifacts/deferred-work.md` — carry-forward items. The FarmDashboardPage clickable-card keyboard-a11y item is owned by 5.10e3 (this story). The `bg-info text-white` contrast item is owned by a post-MVP theming pass (NOT 5.10e3). The 3 security/realtime items are re-deferred to post-MVP (NOT 5.10e3).
- `docs/implementation-artifacts/epic-5-context.md` — compiled epic context (reuse if valid; see step-01 rules).
- `docs/architecture.md` — frontend component structure, CSS conventions. Read for the established responsive patterns.
- `docs/PRD.md` — NFR-5 (mobile 320px+ — this IS 5.10e3's concern), NFR-9 (Performance: <3 seconds on 3G — 5.10e2's concern, DONE), UX Design Principles (consistency, intuitive workflows).
- `docs/ux-specification.md` — §"Responsive Design" / §"Mobile" (if present — read for any mobile-specific UX guidance). §"Loading States" / §"Animation & Motion" (5.10e3's CSS changes must not break 5.10e1's loading states).

Key design decisions for the spec to resolve:

- **Global CSS vs per-file fixes:** the user decision (2026-08-11) is GLOBAL CSS + targeted per-file fixes. The spec MUST add global rules in `src/css/app.scss` for: (a) dialog min-width override on mobile (`@media (max-width: 599px) { .q-dialog__inner .q-card { max-width: 95vw !important; min-width: 0 !important; } }`), and (b) touch-target sizing for dense round buttons on mobile (`@media (max-width: 599px) { .q-btn.round.dense { min-width: 44px; min-height: 44px; } }`). The spec MUST verify these global rules actually work (test at 320px) and enumerate which dialogs/buttons still need per-file fixes (e.g., scoped-style dialogs where the global rule doesn't penetrate, `size="xs"` buttons that need explicit removal of `size="xs"`).
- **Scoped-style penetration:** Quasar scoped styles add a `data-v-*` attribute selector. The global `app.scss` rule (non-scoped) may have lower specificity than a scoped `.record-sale-dialog[data-v-xxx] { min-width: 600px }` rule. The spec MUST test whether `!important` in the global rule overrides scoped styles. If not, add per-file `@media (max-width: 599px)` overrides inside each scoped style block.
- **Touch-device targeting:** should the touch-target CSS rule use `@media (max-width: 599px)` (viewport-based) or `@media (hover: none) and (pointer: coarse)` (touch-device-based)? Recommend viewport-based (`max-width: 599px`) for consistency with the existing 9-file pattern — touch devices with large screens (tablets in landscape) are less of a concern for 44px targets. Document the decision.
- **Quick-search on mobile:** the header quick-search is hidden on `xs` (`class="gt-xs"`). Is this acceptable, or should 5.10e3 add a mobile search affordance (e.g., a search icon that opens a full-screen search overlay)? Recommend accepting the current hiding for MVP (the nav drawer provides navigation; search is a nice-to-have, not a core mobile flow) — document as a deferred item if the user wants a mobile search. The spec MUST flag this as a design decision.
- **q-table horizontal scroll:** Quasar `q-table` has built-in horizontal scroll. `q-markup-table` (static HTML tables) does NOT. The spec MUST enumerate which files use `q-markup-table` or custom table layouts without a scroll container and wrap them in `<div style="overflow-x: auto">`.
- **No new dependencies:** all fixes use existing CSS patterns. No new npm packages (no `postcss-px-to-viewport` or similar mobile CSS tools unless already in `package.json`). Use plain CSS media queries in `app.scss` and scoped styles.
- **i18n / emojis:** none (hardcoded English, no emojis) — consistent with project convention.

## Planning Artifacts to Load

Authoritative sources (load via compile-epic-context subagent for epic-5-context.md if not already compiled, plus selectively for story-specific constraints):

- docs/epics.md — Story 5.10 ACs (AC6 is the focus for 5.10e3; AC4 was 5.10e1 DONE; AC5 was 5.10e2 DONE; AC9 is 5.10e4; AC8 deferred to post-MVP) and Epic 5 story list
- docs/PRD.md — NFR-5 (mobile 320px+ — this IS 5.10e3's concern), NFR-9 (Performance: <3 seconds on 3G — 5.10e2 DONE), UX Design Principles (consistency, intuitive workflows)
- docs/architecture.md — frontend component structure, CSS conventions, Quasar grid/responsive patterns. Read for the established responsive approach.
- docs/ux-specification.md — §"Responsive Design" / §"Mobile" (if present — read for any mobile-specific UX guidance). §"Loading States" / §8 "Animation & Motion" (5.10e3's CSS changes must not break 5.10e1's loading states).
- docs/implementation-artifacts/spec-5-10e1-ux-polish-and-accessibility.md — 5.10e1 spec (DONE); read for the page-header standardization (`26779c6` across 65 pages) and the a11y fixes. 5.10e3's touch-target fixes MUST NOT remove 5.10e1's aria-labels. The FarmDashboardPage clickable-card keyboard a11y was deferred from 5.10e1 to 5.10e3.
- docs/implementation-artifacts/spec-5-10e2-performance-optimization.md — 5.10e2 spec (DONE); read for the dynamic chart.js import pattern and caching guards. 5.10e3's CSS changes must not break chart rendering or store fetch logic. NOTE the post-review patch `fe553e9` (full-page-reload-on-logout).
- docs/implementation-artifacts/spec-5-10b-navigation-polish-breadcrumbs-and-quick-search.md — 5.10b spec (DONE); read for Breadcrumbs.vue responsive behavior and quick-search `gt-xs` hiding. 5.10e3 MUST verify breadcrumbs work at 320px. NOTE the post-finalization patch `3152db1`.
- docs/implementation-artifacts/spec-5-10c-notifications-system.md — 5.10c spec (DONE); read for notification bell + panel responsive behavior (`$q.screen.xs` q-dialog vs `$q.screen.gt.xs` q-menu). 5.10e3 MUST verify the notification panel works at 320px. NOTE the post-review patch `f5e5fc9`.
- docs/implementation-artifacts/spec-5-10d-help-and-documentation.md — 5.10d spec (DONE); read for help menu responsive behavior. 5.10e3 MUST verify the help menu works at 320px. NOTE the post-review patch `4abd369`.
- docs/implementation-artifacts/epic-5-context.md — compiled epic context (reuse if valid; see step-01 rules)
- docs/implementation-artifacts/deferred-work.md — carry-forward items; the FarmDashboardPage clickable-card keyboard-a11y item is owned by 5.10e3 (this story). The `bg-info text-white` contrast item is owned by a post-MVP theming pass (NOT 5.10e3). The 3 security/realtime items are re-deferred to post-MVP (NOT 5.10e3).
- docs/planning-artifacts/sprint-change-proposal-2026-07-28.md — confirms 5.10's dependency on all prior MVP stories

Do NOT load POST-MVP.md as a primary source — it lists deferred modules only. Use it only to confirm a feature is deferred when in doubt.

## Project Conventions (non-negotiable)

- Frontend: Quasar v2.18.5 (Vue 3 + Vite + SSR), `<script setup>` syntax mandatory.
- Backend: Appwrite v21.2.1 (Database, Auth, Storage, Functions).
- State: Pinia. Date/Time: date-fns + date-fns-tz (village timezone from `settingsStore.timezone`, default `Africa/Lusaka`). Charts: Chart.js v4.5.1. Calendar: vue-cal v5 (`^5.0.1-rc.33`).
- Normalized ID-based relationships; composable error handling (useErrorHandler); custom form validation integrated with error handler.
- RBAC: `src/utils/permissions.js`, `src/composables/usePermissions.js` (`hasPermission('<module>:read')`, `hasPermission('<module>:write')`), route guards, PermissionGuard — reuse, do not reinvent. 5.10e2 adds NO new permission keys and changes NO route guards (it is a performance pass on existing pages/stores/composables).
- Dashboard widgets: follow docs/implementation-artifacts/dashboard-widget-pattern.md exactly.
- No new dependencies without verifying they're already in package.json. If a new dep is truly required, HALT with blocking condition `new dependency required: <name> — user approval needed`.
- Match existing code style in src/pages/, src/stores/, src/composables/, src/services/, src/modules/. Read neighboring modules (e.g. src/modules/school/, src/modules/farm/, src/modules/calendar/, src/modules/storage/, src/pages/setup/, src/pages/admin/) before scaffolding.
- i18n: NOT implemented in this project. vue-i18n is NOT installed. All UI strings are hardcoded English, matching existing modules (Epics 1–4 and Stories 5.1–5.14). This is a user-approved decision (2026-07-28); i18n is deferred to post-MVP — see docs/implementation-artifacts/deferred-work.md. Do NOT add vue-i18n. Do NOT use $t() or useI18n(). Write hardcoded English strings consistent with existing modules.
- No emojis in code or UI unless an existing module already uses them (do not remove existing ones).

## Strict Readiness Standard (HALT on any violation)

Before leaving step-02 (plan), the spec MUST satisfy the "Ready for Development" standard from SKILL.md:

- Actionable: every task has a file path and specific action.
- Logical: tasks ordered by dependency.
- Testable: all ACs in Given/When/Then form.
- Complete: no placeholders or TBDs.
- Sufficient: no unresolved requirement/acceptance/dependency/implementation gaps.
- Coherent: no internal contradictions.

HALT with status `blocked` and a precise blocking condition if ANY of these occur:

- A story AC cannot be translated into concrete tasks because a requirement is ambiguous or contradictory between epics.md / PRD.md / ux-specification.md / architecture.md.
- A prerequisite story is not actually `done` (verify against docs/sprint-status.yaml, not against docs/bmm-workflow-status.md which is stale).
- The working tree is dirty or the current branch is an obvious mismatch for Epic 5 work.
- A new third-party dependency is required.
- An AC requires functionality that belongs to a deferred story (5.5/5.6/5.8) or a not-yet-built story — split the work cleanly and exclude the forward-dep portion; if the AC cannot be satisfied without it, HALT.
- The intent resolves to multiple independently shippable goals that cannot be scoped into one spec — set `warnings: [multiple-goals]` in frontmatter and proceed only if they're genuinely inseparable; otherwise HALT. (5.10e3 is AC6 — 320px audit + global CSS + dialog fixes + 44px touch-target fixes + no-wrap flex rows + table scroll + FarmDashboardPage keyboard a11y + MainLayout header audit. Each sub-AC (6a-6f) caps its fix set. The GLOBAL CSS approach (user decision 2026-08-11) reduces the per-file fix count. If the spec derivation finds the total fix count still exceeds one iteration even with the global CSS approach, HALT with a proposal to split 5.10e3 into 5.10e3a (320px audit + global CSS + dialog fixes + no-wrap + tables — 6a/6c/6d/6f) and 5.10e3b (44px touch targets + FarmDashboardPage keyboard a11y — 6b/6e) — do NOT silently expand scope. The broader 5.10 was split into 5.10a–5.10e per user decision 2026-08-04, and 5.10e was split into 5.10e1–5.10e4 per user decision 2026-08-11.)

Do NOT invent requirements. Do NOT pull scope from other stories into this iteration. Do NOT implement features that belong to a later story in the dependency order.

## Implementation Discipline (step-03)

- Follow the spec's task list in order. Do not reorder.
- Reuse existing composables, stores, services, and RBAC utilities. Do not duplicate. (5.10e3 reuses the existing Quasar grid system, `$q.screen` breakpoints, Quasar breakpoint classes (`lt-md`/`gt-xs`), and the existing `@media (max-width: 599px)` pattern already used in 9 files.)
- Vue 3 `<script setup>` only. No Options API. No `this`.
- SSR-safe: 5.10e3 modifies CSS (`src/css/app.scss` and scoped `<style>` blocks), adds HTML attributes (`role`, `tabindex`, `@keydown` handlers on FarmDashboardPage cards), and may remove `no-wrap` classes. All CSS changes are SSR-safe (CSS is not hydration-sensitive). The `@keydown` handlers on FarmDashboardPage cards are client-only interactions (no SSR impact). No new `tables.listRows`, realtime, or Functions calls are introduced.
- Quasar components for all UI primitives. No raw HTML controls. (5.10e3 changes CSS rules and adds a11y attributes — no new UI primitives are introduced.)
- 5.10e3 is REUSE-ONLY: it adds NO new Appwrite infrastructure (no tables, no server functions, no permissions, no `.env` vars, no stores, no pages, no routes). The frontend artifacts modified are `src/css/app.scss` (global responsive CSS), a bounded set of existing component templates (FarmDashboardPage keyboard a11y, no-wrap flex row changes), and a bounded set of scoped `<style>` blocks (dialog min-width media queries). The spec MUST include tasks for all modifications.
- CSS approach: 5.10e3 uses a GLOBAL CSS approach (in `src/css/app.scss`) for the bulk of dialog min-width and touch-target fixes, supplemented by TARGETED per-file fixes for edge cases (scoped-style dialogs, `size="xs"` buttons, no-wrap flex rows, FarmDashboardPage keyboard a11y). The spec MUST verify the global CSS rules actually work at 320px (test with Chrome DevTools mobile emulation) and enumerate which files still need per-file fixes.
- Date handling: N/A for 5.10e3 (no date logic changes — this is a mobile-responsiveness pass).
- Permission checks: 5.10e3 adds NO new permission keys and changes NO route guards. It does NOT alter which users can see which pages — only how those pages render on mobile.
- No emojis in code or UI unless an existing module already uses them (do not remove existing ones).

## Review (step-04) — Full Adversarial

Run the full adversarial review pass per the skill's step-04:

- Blind Hunter: does the implementation actually satisfy each AC as written, with no hidden gaps?
- Edge Case Hunter: walk every branching path and boundary.
- Acceptance Auditor: map each AC to concrete code/test evidence; flag any AC with no evidence.

### Review invariants for Story 5.10e3 <<< CHANGE PER ITERATION >>>

Specific invariants the review MUST verify for 5.10e3:

- **320px dialog fixes (6a):** the global CSS rule in `src/css/app.scss` constrains all `q-dialog` cards on mobile (`@media (max-width: 599px)` with `max-width`/`min-width` overrides using `!important`); the global rule is verified to override inline `style="min-width: ..."` attributes on dialogs (ResidentForm, HouseholdForm, TransactionForm, AddFundingDialog, etc.); for scoped-style dialogs (RecordSaleDialog, HarvestEntryDialog) where the global rule may not penetrate scoped styles, per-file `@media (max-width: 599px)` overrides are added inside the scoped `<style>` block; all 20+ enumerated dialogs render correctly at 320px viewport (no horizontal overflow); the spec documents which dialogs are fixed by the global rule vs which needed per-file fixes.
- **44px touch-target fixes (6b):** the global CSS rule in `src/css/app.scss` increases touch-target size for dense round buttons on mobile (`@media (max-width: 599px)` with `min-width: 44px`/`min-height: 44px`); the global rule is verified to not break desktop layouts (only applies on mobile); `size="xs"` buttons (FarmAlertsPage dismiss, TimetableCellEditor clear) are fixed per-file (remove `size="xs"` or add padding overrides); the spec enumerates which buttons are fixed by the global rule vs which needed per-file fixes; 5.10e1's aria-labels on icon-only buttons are NOT removed or broken by the touch-target sizing changes.
- **No-wrap flex row fixes (6c):** the 3 enumerated no-wrap flex rows (FinanceTransactionsPage:164, TeachersListPage:67, InventoryDetailPage:351) are fixed (either `no-wrap` removed or mobile wrapping override added); the layout still looks correct on desktop (no-wrap preserved on wider screens if a media query approach is used); no horizontal overflow occurs at 320px on these rows.
- **Table horizontal-scroll (6d):** any `q-markup-table` or custom table layouts without a scroll container are wrapped in `<div style="overflow-x: auto">`; `q-table` instances are verified to already have built-in horizontal scroll; the spec enumerates which tables needed wrapping.
- **FarmDashboardPage keyboard a11y (6e):** the clickable module-navigation cards (`FarmDashboardPage.vue:217-238`) now have `role="button"`, `tabindex="0"`, `@keydown.enter`/`@keydown.space.prevent` handlers, and `:focus-visible` styling; the cards are reachable via keyboard (Tab), activated via Enter/Space, and have a visible focus indicator; the click behavior (router.push) is unchanged.
- **MainLayout mobile header (6f):** the header elements work at 320px (hamburger menu, notifications bell, help button, user profile all fit); the notification panel (q-dialog on xs, `max-width: 90vw`) and help menu work at 320px; the quick-search hiding on xs (`gt-xs`) is documented as accepted or a mobile search affordance is added; the spec documents the 320px header layout.
- **No new Appwrite infrastructure:** NO new tables, server functions, permissions, `.env` vars, stores, pages, or routes were added. Verify by checking `setup-appwrite.js`, `seed-roles.js`, `permissions.js`, `.env.example`, `src/stores/` (no NEW store files), `src/router/routes.js`, and `src/pages/` for any 5.10e3-related additions (there should be NONE — `src/css/app.scss` is an EXISTING file being modified, not a new file).
- **No backend changes:** no server functions modified, no `appwrite.config.json` changes, no seed data changes.
- The 5.10b quick-search (hidden on xs — verify acceptable), 5.10c notifications bell + badge + panel + realtime subscription, 5.10d help icon + menu + 20 tooltips, 5.10e1 loading states + error handling + a11y fixes (aria-labels NOT removed by touch-target sizing), 5.10e2 caching guards + dynamic chart.js imports (chart rendering NOT broken by CSS changes), `expandedSections` auto-expand watcher, breadcrumbs (responsive at 320px), the 5.10a dashboard real-data wiring, the 5.11 empty-state banners, the 5.9 module management, the 5.12/5.13 user/role management, the 5.14 password flows, and all prior module dashboards/pages are NOT broken — 5.10e3 only changes CSS rules, adds keyboard a11y attributes, and adjusts flex-wrap behavior; it does not change any page's data flow, permissions, or JavaScript logic.
- NO performance/caching/lazy-loading changes appear in 5.10e3 (that's AC5 / 5.10e2, DONE). NO accessibility/aria-label/contrast changes appear in 5.10e3 EXCEPT the FarmDashboardPage clickable-card keyboard a11y (6e, deferred from 5.10e1). NO testing-checklist doc is produced in 5.10e3 (that's 5.10e4). NO security/realtime fixes appear in 5.10e3 (re-deferred to post-MVP). NO `bg-info text-white` contrast fix appears in 5.10e3 (post-MVP theming pass).
- If the spec set `warnings: [multiple-goals]` and HALTed to propose splitting 5.10e3 into 5.10e3a/5.10e3b, the review verifies the HALT was clean and no partial implementation was left.

**5.10 sub-story roadmap (for context — do NOT implement future sub-stories in this iteration):**

- **5.10a (DONE 2026-08-04):** Dashboard Completion — Real Data Wiring (AC1). Replaced placeholder data, verified widgets functional, <2s load.
- **5.10b (DONE 2026-08-05):** Navigation Polish — Breadcrumbs and Quick Search (AC2). Added breadcrumbs to detail/form/create/edit pages (Module → List → Detail, responsive), global header quick search with grouped results dropdown, fixed active highlighting (auto-expand nav section for active child route), clean-menu audit (removed dead `/communications` link). Post-finalization patch `3152db1` corrected a defective learner search (learners table has no name columns — reworked to search residents then resolve learner rows), the finance result field, and a header-search focus issue.
- **5.10c (DONE 2026-08-05):** Notifications System (AC3). New Appwrite `notifications` + `notification_reads` tables, `createNotification` server function (role-targeted delivery), notifications store, bell icon + count badge in MainLayout header, notification panel with filter by type and mark-as-read, Appwrite realtime live updates, and a bounded set of REAL role-targeted triggers (at-risk learner newly flagged → school roles; farm alert newly raised → `farm:read`; new vendor created → finance/farm roles). In-app only (email deferred). Post-review patch `f5e5fc9` fixed single-role users in `Query.or`. This was the only 5.10 sub-story that added new Appwrite infrastructure.
- **5.10d (DONE 2026-08-10):** Help and Documentation (AC7). Help icon in header (between bell and user profile), responsive help menu (q-menu desktop / q-dialog mobile) with User Guide / FAQ / About, new `/help` page with User Guide tab (12 sections, module-enabled-aware for Farm/School/Vendors) and FAQ tab (17 Q&A across 5 categories), and exactly 20 enumerated `q-tooltip` elements across MainLayout.vue and 9 other existing pages. Reuse-only — no new Appwrite infrastructure. Post-review patch `4abd369` fixed the help button header placement. Review Triage Log §2026-08-10 is authoritative for the current code state.
- **5.10e (split into 5.10e1–5.10e4 per user decision 2026-08-11):** UX Polish, Performance, Mobile Responsiveness, and Final Testing Checklist (AC4, AC5, AC6, AC9):
  - **5.10e1 (DONE 2026-08-11):** UX Polish and Accessibility (AC4). Consistent UI (3 heading-hierarchy fixes + post-finalization patch `26779c6` standardizing page headers across 65 pages + `6f251e7` page margins), 9 loading-state additions, 8 error-handling standardizations, 10 success-notification standardizations, 17 accessibility fixes (icon-only aria-labels, notification-badge live region, bounded `bg-warning text-dark` contrast fix), and the 2 deferred 5.10b a11y fixes (Breadcrumbs back-button destination-specific aria-label, quick-search keyboard nav via `highlightedIndex`/`@keydown` preserving `no-focus no-refocus`). Reuse-only. 1 deferred item (FarmDashboardPage clickable-card keyboard a11y → 5.10e3 per user decision 2026-08-11). `bg-info text-white` contrast deferred to post-MVP theming pass. Review Triage Log §2026-08-11 is authoritative for the current code state; `final_revision: 6f251e7`.
  - **5.10e2 (DONE 2026-08-11):** Performance Optimization (AC5). Lazy-loading audit (2 static chart.js imports → dynamic `await import()`), bundle size check (documented top 15 chunks; chart.js relocated to shared on-demand chunk), caching (bounded `loaded`/`invalidate` guards on 5 store actions across 4 files — households, residents, inventory general items, finance categories/funding sources; page/limit-keyed guards for paginated stores; CUD actions force-bypass; filter setters invalidate), removed the 300ms `setTimeout` in `useDashboardData.js`, documented 3G throttle time-to-interactive estimates (~1.95-1.97s per key page, calculated from gzip sizes). Reuse-only. Review: 0 patches, 3 deferred (low — pre-existing chart.js dynamic-import error-handling gap class shared by 9 files), 13 rejected. Post-review patch `fe553e9` (full-page-reload-on-logout to clear cached Pinia store state — fixes stale-data-across-sessions bug class for ALL cached stores, surfaced during Epic 5 review preceding 5.10e3). The 5.10e2 spec's `baseline_revision` is `0f9458e`; its Review Triage Log §2026-08-11 is authoritative for the current code state. Dep: 5.10e1. DONE 2026-08-11.
  - **5.10e3 (THIS ITERATION):** Mobile Responsiveness (AC6). Full 320px audit of ALL 66 pages + all fixes (global CSS in `app.scss` + targeted per-file fixes for scoped-style dialogs), 44px touch-target audit (per user decision 2026-08-11: full audit + all fixes; global CSS for dense round buttons + per-file for `size="xs"`), FarmDashboardPage clickable-card keyboard a11y (per user decision 2026-08-11: folded into 5.10e3), no-wrap flex row fixes, table horizontal-scroll audit. Reuse-only.
  - **5.10e4:** Final Testing Checklist (AC9). Manual final-testing-checklist.md doc covering all MVP user journeys, RBAC matrix, data integrity, integrations, sample-data mode (per user decision 2026-08-11: manual checklist only; automated test infrastructure deferred to a new post-MVP testing epic — 5.10e4 documents the new epic in deferred-work.md). This is the FINAL 5.10 sub-story and the FINAL Epic 5 story — completing it marks 5.10 and Epic 5 as done.
- **AC8 (System Health Monitoring): DEFERRED to post-MVP** (user decision 2026-08-04). Documented in `deferred-work.md` during 5.10a; 5.10e4 will verify/expand the deferral entry (proposed admin page layout, server function requirements, data sources, estimated post-MVP effort). Not implemented in MVP.

**Prior Epic 5 story summaries (for regression-checking context):**

**5.9 (Module Management):** Admin page at `/admin/modules`. Core modules always enabled (Residents, Households, Finance, Inventory, Calendar, Storage). Optional MVP modules toggleable: Farm, School, Vendors ONLY (NOT Guests/Equipment/Energy — deferred). Toggle hides nav/widgets but preserves data. Dependency warning on disable. Updates `settingsStore.modulesEnabled`. Dep: all MVP previous stories.

**5.14 (Auth Completeness):** ProfilePage "Change Password" dialog. AuthPage "Forgot password?" link → `Account.createRecovery` → email link → `/auth/reset-password` page → `Account.updateRecovery`. Email verification deferred. No self-service signup. Deps: 1.3, 1.11.

**5.12 (User CRUD):** UsersPage `/admin/users` "Add User" button (System Admin only). Server-side Appwrite Function for admin-scope user creation. Soft-deactivate. Cannot deactivate self or last System Administrator. Audit logging. Deps: 1.4, 1.11.

**5.13 (Role Assignment UI):** UsersPage "Manage Roles" dialog. "View Permissions" shows effective permission union. `/admin/roles` page: role list with permission matrix. Read-only for MVP. `seed-roles.js` upsert-capable. Dep: 5.12.

**5.11 (Start Fresh Wizard):** SetupWizard "Start Fresh" card enabled. 5-step wizard: Village Profile → Admin User → Village Head → Module Selection → First Household. Sets `is_using_sample_data = false`. Empty-state CTAs on dashboard and list pages. Last-System-Admin guard added to `updateUser`. Deps: 5.9, 5.12. DONE 2026-08-04.

**5.10a (Dashboard Completion — Real Data Wiring):** Replaced `DashboardPage.vue` placeholder data with real, permission-gated Appwrite fetches via new `src/composables/useDashboardData.js` (household/resident counts via `Query.limit(1)` + `.total`; finance totals via `financeStore.fetchSummary()`; recent activity via per-module `tables.listRows` with `Query.orderDesc('$createdAt')` + `Query.limit(3)`, merged/sorted/sliced to 8). Extended `QuickStatsWidget` with Total Income/Total Expense cards. Each module fetch isolated (try/catch) so one failure doesn't break the dashboard. SSR-safe (onMounted only). AC8 (System Health Monitoring) deferred to post-MVP and documented in `deferred-work.md`. NOTE: a 300ms `setTimeout` hydration delay remains in `useDashboardData.load()` — owned by 5.10e2 (performance) for removal. Dep: all prior MVP stories. DONE 2026-08-04.

**5.10b (Navigation Polish — Breadcrumbs and Quick Search):** Added reusable `src/components/layout/Breadcrumbs.vue` (responsive Module → List → Detail, mobile back-button-only) replacing ad-hoc Back buttons on 22 detail/form/create/edit pages; added `meta.breadcrumb` to existing routes; added `src/composables/useGlobalSearch.js` (permission-gated, SSR-safe quick search across households/residents/finance/plots/learners/vendors/inventory/calendar + nav items, grouped dropdown); added a route watcher in `MainLayout.vue` to auto-expand the active `expandedSections` key; removed the dead `/communications` nav link. Post-finalization patch `3152db1` (2026-08-05) corrected a defective learner search (the `learners` table has NO `first_name`/`last_name` columns — it joins `residents` via `resident_id`; reworked to search residents first then resolve learner rows), the finance result secondary field (`amount_funded ?? amount` + ZMW currency + date), and a header-search focus issue (`no-focus no-refocus` on the results `q-menu`). The 5.10b spec's `final_revision` is `3152db1`; its Review Triage Log §2026-08-05 is authoritative for the current code state. Dep: 5.10a. DONE 2026-08-05.

**5.10c (Notifications System):** Added new Appwrite `notifications` table (role-targeted, one row per event) and `notification_reads` table (per-user read receipts, row-secured matching the `file_metadata` precedent). New `createNotification` Appwrite Function derives `target_roles` from a hardcoded `TYPE_CONFIG` authorization+targeting matrix (never trusts client-supplied targeting; validates caller permission per type). New `src/stores/notifications-store.js` Pinia store (fetchMyNotifications with role-intersection query + read-receipt join, unreadCount getter, markRead/markAllRead, filterByType, createNotification helper). Bell `q-btn` + `q-badge` + `NotificationPanel.vue` in `MainLayout.vue` header (q-menu desktop, q-dialog mobile). Appwrite realtime subscription with 30s polling fallback. Exactly 3 triggers: at-risk learner newly flagged (school roles, dedup by type+related_entity_type+related_entity_id), farm alert newly raised (farm roles, same dedup), new vendor created (finance/farm roles). Seeded demo notifications in `seedAllData`. Review: 2 patches (1 high — deterministic SHA-256 rowId for race-free dedup; 1 low — missing `delete` permission on `notification_reads`), 3 deferred items (table-level create griefing vector, realtime sync-only error handling, at-risk 60s cache re-flip suppression — all pre-existing-pattern-class; per user decision 2026-08-11 these 3 security/realtime items are re-deferred to post-MVP hardening, NOT owned by any 5.10e sub-story), 7 rejected findings. Post-review patch `f5e5fc9` fixed single-role users in `Query.or` (used `Query.contains` for single role, `Query.or`+`Query.contains` for multiple). Dep: 5.10b. DONE 2026-08-05.

**5.10d (Help and Documentation):** Added a help `q-btn` (`icon="help"`) in `MainLayout.vue` header between the 5.10c notifications bell and the user profile `q-btn`, with a responsive help menu (`q-menu` desktop / `q-dialog` mobile) containing User Guide / FAQ / About (version + village name) links — shared via a new `src/components/layout/HelpMenuList.vue` component (extracted during review patch to deduplicate the menu/dialog markup, mirroring the `NotificationPanel.vue` reuse pattern). Added a new `/help` route (`requiresAuth: true`, no `requiresPermission`, no breadcrumb — top-level page like Dashboard) and a new `src/pages/help/HelpPage.vue` with two `q-tab-panels`: "User Guide" (Getting Started card + 11 `q-expansion-item` module sections, module-enabled-aware for Farm/School/Vendors showing a "not enabled" banner post-hydration) and "FAQ" (17 Q&A across 5 categories: General, Data Entry, Roles & Permissions, Troubleshooting, Sample Data vs Real Data). Added exactly 20 enumerated `q-tooltip` elements: help icon, notifications bell, quick-search input, 7 nav-section headers in `MainLayout.vue`, 7 primary action buttons across 6 list pages, and 3 complex form fields (VillageSettingsPage currency, StorageSettingsPage quota, LongTermGoalsSettingsPage benchmark threshold — substituted for the non-existent at-risk attendance threshold UI control). Reuse-only — no new Appwrite tables, functions, permissions, `.env` vars, or stores. Review: 1 patch (low — `HelpMenuList.vue` extraction), 16 rejected findings (all out-of-scope or factually incorrect), 0 deferred. Post-review patch `4abd369` fixed the help button header placement. The 5.10d spec's `baseline_revision` is `d6e8be0`; its Review Triage Log §2026-08-10 is authoritative for the current code state. Dep: 5.10c. DONE 2026-08-10.

**5.10e1 (UX Polish and Accessibility):** Audit-and-fix pass delivering AC4. 4a Consistent UI: 3 heading-hierarchy fixes (HouseholdsListPage/FinanceReportsPage → `text-h5`, DashboardPage `text-h4` → `text-h5`); post-finalization patch `26779c6` extended this to ALL 65 pages (standardized `<h4 class="text-h5 q-my-none">` + `<p class="text-grey-7 q-mb-none">` subtitle), plus `6f251e7` page-margin alignment on 9 pages. 4b Loading states: 9 pages got loading indicators (InventoryListPage, FarmDashboardPage, CropsListPage, PlantingsListPage, SalesListPage, ClassesListPage, VendorsListPage, AllLoansPage; CalendarPage already covered). 4c Error handling: 8 standardizations (DashboardPage + 10 useDashboardData catch blocks got non-blocking `notifyError`; farm-store 6 catch blocks got `notifyError`; CreatePlantingPage error → `handleError`; VillageSettingsPage 5 catches → `notifyError`; MainLayout realtime-subscribe-failure notified once; lendingStore + notifications-store de-duplicated redundant `console.error`). 4d Success confirmations: 10 CUD actions standardized to `useErrorHandler().notifySuccess()` (PlotFormPage create/update, PlotsListPage delete, CropFormPage create, PlantingDetailPage harvest start/complete, CreatePlantingPage record, InventoryFormPage create/update, InventoryDetailPage sale, MainLayout logout, FinanceDashboardPage export, FinanceTransactionsPage funding add, VillageSettingsPage council member save/remove). 4e Accessibility: 17 fixes (5 icon-only aria-labels each on HouseholdsListPage + ResidentsListPage, 3 on PlotsListPage, 2 on LearnersListPage, 1 on LongTermGoalsSettingsPage back button, 1 on FinanceTransactionsPage inventory-link, 3 refresh-button aria-labels on farm widgets, notification-badge `role="status"` + `aria-live="polite"`, 6 `bg-warning text-white` → `text-dark` contrast fixes). 4f Deferred a11y: Breadcrumbs.vue `backAriaLabel` computed (destination-specific, derived from `props.items`); MainLayout quick-search keyboard nav via `highlightedIndex` ref + `flattenedResults` computed + `@keydown` handler (ArrowDown/ArrowUp/Enter/Escape) preserving `no-focus no-refocus`. Reuse-only — no new Appwrite infra. Review: 1 patch (medium — malformed `deferred-work.md` entry corrected), 1 deferred (medium — FarmDashboardPage clickable-card keyboard a11y → 5.10e3 per user decision 2026-08-11), 19 rejected. `bg-info text-white` contrast (~1.8:1, 10+ files) deferred to post-MVP theming pass. The 5.10e1 spec's `baseline_revision` is `2acced5`; `final_revision` is `6f251e7`; its Review Triage Log §2026-08-11 is authoritative for the current code state. Dep: 5.10d. DONE 2026-08-11.

**5.10e2 (Performance Optimization):** Audit-and-fix pass delivering AC5. 5a Lazy-loading: converted the 2 remaining static chart.js imports to dynamic `await import()` — `FinanceReportsPage.vue` (static `import { Chart, registerables } from 'chart.js'` + module-scope `Chart.register(...)` → cached async `ensureChart()` helper invoked from `createChart()`) and `InventoryStockWidget.vue` (static `import Chart from 'chart.js/auto'` → dynamic import inside `renderChart()`, now `async`). Verified all 24 routes in `routes.js` still use `() => import(...)` (no static route imports introduced). 5b Bundle size: documented top 15 chunks from `npm run build` (jspdf 390KB, chart 198KB, html2canvas 195KB, index.es 148KB, index 103KB, jszip 94KB, appwrite 83KB, vue-cal 74KB, QIcon 66KB, MainLayout 54KB, farm-store 45KB, FarmDashboardPage 39KB, PlantingDetailPage 37KB, DashboardPage 35KB, FinanceReportsPage 28KB). Post-fix build confirms chart.js is no longer in the FinanceReportsPage or dashboard chunks — it's a shared on-demand `chart-*.js` chunk (198KB). No chunk >250KB on a common navigation path (jspdf/html2canvas are on-demand export-only). 5c Removed the 300ms `setTimeout` hydration delay in `useDashboardData.js:310-311` (introduced by 5.10a as a hydration-safety workaround; unnecessary since fetches run in `onMounted` which is client-only). 5d Caching: added `loaded`-flag cache guards to 5 store actions across 4 files — `households-store.js` (`fetchHouseholds` with page/limit-keyed guard), `residents-store.js` (`fetchResidents` with page/limit-keyed guard; `setSearchFilter`/`setHouseholdFilter`/`clearFilters` invalidate), `inventory-store.js` (`fetchItems` with page/limit-keyed guard; `setFilters`/`clearFilters` invalidate), `finance-store.js` (added read-guard to `fetchCategories`/`fetchFundingSources` using pre-existing `categoriesLoaded`/`fundingSourcesLoaded` flags; `fetchDashboardData` forwards `forceRefresh`). CUD actions force-bypass the cache (`force=true` 3rd arg). Verified `vendors-store`, `farm-store` (plots/crops), `class-store`, `learner-store`, `calendar-store` already cache correctly — left untouched. `notifications-store`/`auth-store`/`settings-store` excluded from caching. No `useStoreCache.js` helper extracted (caching patterns differ between paginated and non-paginated stores). 5e 3G test: documented Slow 3G throttle procedure and calculated time-to-interactive estimates for 6 key pages (~1.95-1.97s each, under the 3s AC5 budget) using gzip sizes from the post-fix build report (methodology: byte-budget calculation, not a live DevTools recording — follow-up manual spot-check recommended for 5.10e4's final testing checklist). Reuse-only — no new Appwrite infrastructure. Review: 0 patches, 3 deferred (low — pre-existing chart.js dynamic-import error-handling gap class shared by 9 files, not caused by this story), 13 rejected (mix of factually-incorrect claims and correct-behavior-mistaken-for-bugs). Post-review patch `fe553e9` (2026-08-11, applied during Epic 5 review preceding 5.10e3): changed `MainLayout.handleLogout()` from `router.push('/auth')` to `window.location.href = '/auth'` (full page reload) to clear all Pinia store state on logout — fixes a stale-data-across-sessions bug class where cached `loaded` flags from User A's session would serve User A's data to User B after login on a shared device. This patch fixes ALL cached stores (5.10e2 additions + pre-existing vendors/farm/calendar/class/learner), not just the 5.10e2 additions. The 5.10e2 spec's `baseline_revision` is `0f9458e`; its Review Triage Log §2026-08-11 is authoritative for the current code state. Dep: 5.10e1. DONE 2026-08-11.
