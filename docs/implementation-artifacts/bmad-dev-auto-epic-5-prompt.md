# /bmad-dev-auto Prompt — Epic 5 Final Story (5.10 Sub-Stories: 5.10a → 5.10e, with 5.10e split into 5.10e1 → 5.10e4) — 5.10e4 is the FINAL Epic 5 story

> **Usage:** Copy everything below the `---` line into a fresh `/bmad-dev-auto` invocation.
> **Adapting for subsequent iterations:** Change ONLY the three sections marked
> `<<< CHANGE PER ITERATION >>>` — (1) Current Iteration Target, (2) Story X.Y Specifics,
> (3) Review invariants. Everything else is campaign-level context that stays constant.
> The MVP dependency order in "Epic 5 MVP Scope" tells you which sub-story is next after
> each completion.
>
> **Sub-story split (user decision 2026-08-04):** Story 5.10 "System Completion" has 9 broad
> ACs spanning 6+ independently shippable features. Per the bmad-dev-auto discipline rules
> ("HALT if the intent resolves to multiple independently shippable goals"), 5.10 is split
> into sub-stories 5.10a–5.10e, each processed in its own iteration. AC8 (System Health
> Monitoring) is deferred to post-MVP with thorough documentation (user decision 2026-08-04).
> AC3 (Notifications) is included in MVP as sub-story 5.10c (user decision 2026-08-04).
>
> **5.10e sub-split (user decision 2026-08-11):** 5.10e itself covers 4 broad ACs
> (AC4 UX polish/accessibility, AC5 performance, AC6 mobile responsiveness, AC9 final
> testing checklist) — the same multi-goal pattern that caused 5.10 to be split. Per the
> bmad-dev-auto discipline, 5.10e is pre-split into sub-stories 5.10e1–5.10e4, each
> processed in its own iteration. AC9's automated test infrastructure is deferred to a new
> post-MVP testing epic (user decision 2026-08-11); 5.10e4 produces only a manual final
> testing checklist doc for MVP. Completing 5.10e4 marks 5.10 and Epic 5 as done.

---

You are running bmad-dev-auto for the Sustainable Model Village Management System (village-app). This invocation processes EXACTLY ONE story this iteration and HALT cleanly so the next iteration can pick up the next story.

## Current Iteration Target <<< CHANGE PER ITERATION >>>

Epic: 5 — "Village Calendar, Storage, Optional Modules, and User Management"
Story to implement THIS iteration: 5.10e4 — Final Testing Checklist (AC9) — FINAL Epic 5 story
Epic context file to load/compile: {implementation_artifacts}/epic-5-context.md
Spec file to produce: {implementation_artifacts}/spec-5-10e4-final-testing-checklist.md

If the spec file already exists with status `draft`, resume it. If it exists with any other status, do NOT overwrite — HALT with blocking condition `spec already in progress/done; user decision required`.

## Epic 5 MVP Scope (do NOT implement deferred stories)

MVP stories, in dependency order (stories marked ✅ are done; ← THIS ITERATION marks the current target):

1. ✅ 5.1 Village Calendar - Global Calendar with Category Filtering (deps: Epic 1) — DONE 2026-07-28
2. ✅ 5.2 Village Calendar - Role-Based Event Creation and Editing (deps: 5.1) — DONE 2026-07-29
3. ✅ 5.3 Cloud Storage - Role-Based Storage Quotas and Personal Folders (deps: 1.10) — DONE 2026-07-30
4. ✅ 5.4 Cloud Storage - Shared Folders and Module-Based Access (deps: 5.3) — DONE 2026-07-31
5. ✅ 5.7 Vendors/Suppliers Management Module (deps: 2.2, 2.3, 3.8 — all done) — DONE 2026-08-01
6. ✅ 5.9 Module Management and Configuration (deps: all MVP previous) — DONE 2026-08-03
7. ✅ 5.14 Authentication Completeness - Password Change and Reset (deps: 1.3, 1.11) — DONE 2026-08-03
8. ✅ 5.12 User Management - CRUD Operations (deps: 1.4, 1.11) — DONE 2026-08-03
9. ✅ 5.13 Role Assignment and Permissions Management UI (deps: 5.12) — DONE 2026-08-04
10. ✅ 5.11 Start Fresh Production Setup Wizard (deps: 5.9, 5.12) — DONE 2026-08-04
11. 5.10 System Completion — split into sub-stories (user decision 2026-08-04; 5.10e re-split 2026-08-11):
    - ✅ 5.10a Dashboard Completion — Real Data Wiring and Widget Finalization (AC1) — DONE 2026-08-04
    - ✅ 5.10b Navigation Polish — Breadcrumbs and Quick Search (AC2) — DONE 2026-08-05 (post-finalization patch `3152db1` applied; see spec-5-10b Review Triage Log §2026-08-05)
    - ✅ 5.10c Notifications System (AC3) — includes new Appwrite `notifications` table — DONE 2026-08-05 (post-review patch `f5e5fc9` applied; see spec-5-10c Review Triage Log §2026-08-05)
    - ✅ 5.10d Help and Documentation (AC7) — DONE 2026-08-10 (post-review patch `4abd369` applied; see spec-5-10d Review Triage Log §2026-08-10)
    - 5.10e UX Polish, Performance, Mobile Responsiveness, and Final Testing Checklist — split into sub-stories (user decision 2026-08-11):
      - ✅ 5.10e1 UX Polish and Accessibility (AC4) — DONE 2026-08-11 (post-finalization patches `26779c6` page-header standardization + `6f251e7` page margins applied; see spec-5-10e1 Review Triage Log §2026-08-11)
      - ✅ 5.10e2 Performance Optimization (AC5) — DONE 2026-08-11 (post-review patch `fe553e9` full-page-reload-on-logout to clear cached Pinia store state applied; see spec-5-10e2 Review Triage Log §2026-08-11)
      - ✅ 5.10e3 Mobile Responsiveness (AC6) — DONE 2026-08-12 (`cde237d`; 1 low patch — CSS var fallback for focus outline; 15 rejected findings; see spec-5-10e3 Review Triage Log §2026-08-12). NOTE residual risk: manual 320px visual/interactive verification was NOT performed in 5.10e3 (only `npm run build`) — 5.10e4's final-testing-checklist.md absorbs these manual mobile checks per user decision 2026-08-12.
      - 5.10e4 Final Testing Checklist (AC9 — manual doc; automated test infra deferred to new post-MVP testing epic) ← THIS ITERATION — FINAL Epic 5 story
    - AC8 System Health Monitoring — DEFERRED to post-MVP (user decision 2026-08-04; documented in deferred-work.md during 5.10a; 5.10e4 will verify/expand the deferral entry)

DEFERRED (post-MVP — out of scope, do NOT implement, do NOT add toggles for them): 5.5 Guests, 5.6 Equipment, 5.8 Energy, 4.9–4.11, 5.10 AC8 (System Health Monitoring).

## Story 5.10e4 Specifics <<< CHANGE PER ITERATION >>>

Intent: Deliver the Final Testing Checklist sub-story for MVP Story 5.10 — the FINAL Epic 5 story. Implement Story 5.10 AC9: "Final testing checklist: all user journeys tested, RBAC enforced, data integrity validated, all integrations working, sample data mode functional." Per user decision 2026-08-11, AC9's "final testing checklist" is satisfied in MVP by a MANUAL `docs/implementation-artifacts/final-testing-checklist.md` doc (produced by this story) covering all MVP user journeys, RBAC matrix, data integrity, integrations, and sample-data mode. The full automated test infrastructure (Vitest + Cypress/Playwright + comprehensive tests) is DEFERRED to a new post-MVP testing epic — the project has NO existing test infrastructure (`package.json` `test` script is a placeholder `echo "No test specified" && exit 0`, zero test devDependencies), and setting it up is large scope that warrants its own epic. 5.10e4 documents this new post-MVP testing epic in `deferred-work.md` and in the final-testing-checklist.md itself. Per user decision 2026-08-12, 5.10e4's checklist ALSO absorbs the manual 320px mobile checks and 44px touch-target verification that 5.10e3 could not perform (its residual risk: only `npm run build` was run, no manual visual/interactive verification at 320px), plus the manual 3G performance spot-checks from 5.10e2 (which documented time-to-interactive estimates from gzip sizes but did not perform live 3G throttling). This is Story 5.10 AC9 only — AC4 (UX polish/accessibility) was 5.10e1 (DONE), AC5 (performance) was 5.10e2 (DONE), AC6 (mobile responsiveness) was 5.10e3 (DONE). AC8 (System Health Monitoring) is deferred to post-MVP and 5.10e4 expands its deferral entry.

This is a DOC-ONLY story — NO code changes whatsoever (no .vue, no .js, no .scss, no Appwrite, no package.json). The deliverables are: (1) a NEW `docs/implementation-artifacts/final-testing-checklist.md` manual QA checklist doc, (2) an UPDATE to `docs/implementation-artifacts/deferred-work.md` (expand the AC8 deferral entry + document the new post-MVP testing epic), and (3) an UPDATE to `docs/sprint-status.yaml` during finalization (flip `5-10e4` → `done`, `5-10` → `done`, AND `epic-5` → `done` per user decision 2026-08-12 — Epic 5 is the first epic to flip to `done` since all its MVP stories are complete; the optional `epic-5-retrospective` remains `optional`). The existing `docs/testing.md` (manual testing checklists for Stories 1.4 and 1.8 only) is a narrow planning doc, NOT a system-wide final checklist — 5.10e4's `final-testing-checklist.md` supersedes it for MVP sign-off (do NOT delete `docs/testing.md`; reference it as a precursor).

ACs (from docs/epics.md Story 5.10 AC9 — treat as authoritative):

9. Final testing checklist: all user journeys tested, RBAC enforced, data integrity validated, all integrations working, sample data mode functional.

Sub-ACs for 5.10e4 (derived from AC9):

9a. **MVP user journeys checklist.** The checklist MUST enumerate every MVP user journey organized by module/feature area, with a checkbox per journey. The journeys MUST cover ALL MVP modules: Residents (1.7), Households (1.6), Finance — income/expense/categories/funding/loans (2.1–2.5), Inventory — core + auto-from-purchases (2.6–2.7), Financial Reports (2.8), Finance Dashboard (2.9), Farm — plots/crops/plantings/harvest/sales/profitability/yield/alerts (3.1–3.10), School — learners/tests/attendance/at-risk/interventions/long-term-goals/progress-reports (4.1–4.2, 4.6–4.8, 4.12–4.13), School Calendar — terms/holidays/bell-schedules/timetable (4.3–4.5), Village Calendar (5.1–5.2), Cloud Storage — personal + shared (5.3–5.4), Vendors (5.7), Module Management (5.9), Dashboard (5.10a), Navigation — breadcrumbs + quick-search (5.10b), Notifications (5.10c), Help/Docs (5.10d), UX polish (5.10e1), Performance (5.10e2), Mobile responsiveness (5.10e3), Start Fresh wizard (5.11), User CRUD (5.12), Role assignment UI (5.13), Auth — password change/reset (5.14), Village Settings (1.8), Profile (1.11). Each journey: role(s) that can perform it, key steps, expected result, checkbox. The spec MUST derive the journey list from the actual routes in `src/router/routes.js` (24 routes) and the actual pages in `src/pages/` + `src/modules/*/pages/` (66 .vue page files) — do NOT invent journeys for deferred modules (5.5 Guests, 5.6 Equipment, 5.8 Energy, 4.9–4.11).

9b. **RBAC enforcement matrix.** The checklist MUST include a role × permission × page matrix verifying that each of the seeded roles (System Administrator, Village Head, Finance Manager, Events Coordinator, Resident, and any others in `server/scripts/seed-roles.js`) can access exactly the pages/operations they should, and are blocked from the rest. The matrix MUST be derived from `src/utils/permissions.js` (the canonical permission keys), `src/router/routes.js` (`requiresPermission` meta), `src/composables/usePermissions.js` (`hasPermission`/`hasAnyPermission`/`hasAllPermissions`), and `PermissionGuard` usage in components. The checklist MUST include manual verification steps for: (1) route guards (unauthenticated → `/auth`, missing permission → `/unauthorized`), (2) in-page conditional rendering (buttons/menus hidden when permission absent), (3) the 5.13 Roles page read-only permission matrix display, (4) the 5.12 self-deactivate / last-System-Admin guards, (5) module-toggle hiding nav for disabled modules (5.9). The spec MUST enumerate the exact permission keys (e.g. `residents:read`, `finance:write`, `farm:read`, `school:read`, `storage:read`, `users:write`, `*` for admin) and which roles grant each.

9c. **Data integrity validation checklist.** The checklist MUST enumerate every cross-module data-integrity invariant and provide a manual verification step for each. Known invariants the spec MUST include (verify against actual code, add any others found): (1) Finance expense with category "Purchase" auto-creates an Inventory item (2.7); (2) Farm harvest completion auto-creates an Inventory item (3.7); (3) Farm sale recording creates a Finance income transaction AND decrements Inventory (3.8); (4) Vendor appears in Farm sales buyer dropdown AND Finance expense vendor dropdown (5.7); (5) At-risk learner newly flagged fires a notification to school roles (5.10c); (6) Farm alert newly raised fires a notification to farm roles (5.10c); (7) New vendor created fires a notification to finance/farm roles (5.10c); (8) Notification dedup key (type+related_entity_type+related_entity_id) prevents duplicate notifications (5.10c); (9) Start Fresh wizard sets `is_using_sample_data = false` and shows empty-state CTAs (5.11); (10) Sample-data wipe routes to the Start Fresh wizard (1.9 AC8 cross-ref → 5.11); (11) Household-before-resident ordering in empty-state CTAs (1.7 AC4 → 5.11); (12) Soft-deactivate preserves audit history; cannot deactivate self or last System Admin (5.12); (13) Finance currency formatting uses village settings `currency_symbol` (1.8); (14) Date/time formatting uses village settings `timezone` via date-fns-tz (1.8); (15) Module disable hides nav/widgets but preserves data (5.9); (16) Storage quota enforced at upload time, warning above 90% (5.3); (17) Personal files private per user, shared folders role-based (5.3–5.4); (18) Full-page-reload-on-logout clears cached Pinia store state (5.10e2 patch `fe553e9`). The spec MUST verify each invariant still holds by tracing the code path (file:function references) and provide a manual reproduction step.

9d. **Integrations checklist.** The checklist MUST enumerate every external integration and provide a manual verification step: (1) Appwrite Database — all MVP tables listed in `server/scripts/setup-appwrite.js` exist and accept CRUD; (2) Appwrite Auth — login/logout/password-change/password-reset flows (5.14); (3) Appwrite Storage — personal + shared buckets, file-level security (5.3–5.4); (4) Appwrite Functions — `createAdmin` (1.3), admin-scope user creation (5.12), `createNotification` (5.10c), `storageUsageReport` (5.4); (5) Appwrite Realtime — notifications live update + 30s polling fallback (5.10c), finance dashboard realtime (5.10a); (6) Chart.js v4.5.1 — dynamic imports render correctly on FinanceReportsPage + InventoryStockWidget + dashboard widgets (5.10e2); (7) vue-cal v5 — Village Calendar + School timetable rendering (5.1, 4.5); (8) date-fns + date-fns-tz — all date/time formatting respects village timezone (1.8); (9) Pinia store caching guards — households/residents/inventory/finance categories/funding sources (5.10e2); (10) Quasar grid + `$q.screen` breakpoints — responsive layout across all pages (5.10e3). For each, the checklist provides a manual smoke step and expected result.

9e. **Sample data mode checklist.** The checklist MUST verify the full sample-data lifecycle: (1) `yarn seed:all` (or equivalent) populates Katete Model Village seed data across all MVP modules; (2) sample-data banner displays with "Start Fresh — Wipe All Data" action; (3) wipe action routes to the Start Fresh wizard (5.11); (4) Start Fresh wizard completes 5 steps and sets `is_using_sample_data = false`; (5) empty-state CTAs appear on dashboard and list pages post-wizard; (6) re-seeding sample data restores the demo state. The spec MUST enumerate the seed scripts (from `package.json` scripts and `server/scripts/seed-*.js`) and the exact verification steps.

9f. **Manual mobile-responsiveness verification (absorbed from 5.10e3 residual risk per user decision 2026-08-12).** The checklist MUST include a dedicated "Mobile Responsiveness (320px)" section with manual verification steps for: (1) open each of the 20+ dialogs enumerated in 5.10e3 (ResidentForm, HouseholdForm, TransactionForm, RecordSaleDialog, HarvestEntryDialog, FinanceSettingsPage, AddFundingDialog, ClassDetailPage, BellSchedulesSettingsPage, VillageSettingsPage, FinanceTransactionsPage, EventFormDialog, CalendarEventsSettingsPage, TeachersListPage, ClassesListPage, AcademicTermsSettingsPage, UpdateStatusDialog, PlantingsListPage, HouseholdsListPage, LearnerAcademicsTab) at 320px in Chrome DevTools mobile emulation — verify no horizontal overflow, card ≤95vw; (2) verify dense round header/nav buttons (hamburger, notifications bell, help, user avatar) render at ≥44px × 44px on mobile; (3) verify the 3 no-wrap rows (FinanceTransactionsPage:164, TeachersListPage:67, InventoryDetailPage:351) wrap instead of overflow at 320px; (4) verify FarmDashboardPage module cards are keyboard-navigable (Tab → focus visible, Enter/Space → navigate); (5) verify MainLayout header fits at 320px (hamburger + bell + help + avatar; quick-search hidden on xs is acceptable); (6) verify notification panel and help menu open within viewport at 320px. These are the manual checks 5.10e3 could not perform (its `npm run build` only verified compilation, not runtime rendering).

9g. **Manual performance spot-checks (absorbed from 5.10e2 per user decision 2026-08-12).** The checklist MUST include a "Performance" section with manual verification steps for: (1) Chrome DevTools 3G throttling — load Dashboard, FinanceReportsPage, FarmDashboardPage, School dashboard — verify time-to-interactive is <3 seconds (5.10e2 documented estimates of ~1.95–1.97s from gzip sizes; the manual check confirms the estimate); (2) verify chart.js chunks load on-demand (Network tab shows `chart-*.js` loaded only when a chart page is visited, not on initial load); (3) verify route lazy-loading (Network tab shows page chunks loaded on navigation, not upfront); (4) verify Pinia cache guards (navigate to HouseholdsListPage twice — second load should not re-fetch if no filter changed; CUD action forces re-fetch); (5) verify full-page-reload-on-logout clears cached state (login as user A, logout, login as user B — no stale user-A data visible).

9h. **AC8 deferral expansion (in deferred-work.md).** 5.10e4 MUST update the existing AC8 deferral entry in `docs/implementation-artifacts/deferred-work.md` (currently a single paragraph under "Deferred from: Story 5.10a planning (2026-08-04)") to expand it with: (1) proposed admin page layout (which metrics on the page, e.g. database size, storage usage, active user counts, error logs); (2) server function requirements (which Appwrite project-level metrics are needed, that the client SDK does not expose); (3) data sources (where each metric would come from); (4) estimated post-MVP effort (rough T-shirt sizing). This is a documentation update to `deferred-work.md` — NOT implementation of AC8.

9i. **Post-MVP testing epic documentation (in deferred-work.md + final-testing-checklist.md).** 5.10e4 MUST add a new entry to `docs/implementation-artifacts/deferred-work.md` documenting the new post-MVP testing epic: scope (Vitest for unit tests of stores/composables/utils, Cypress or Playwright for E2E of the journeys in 9a, CI integration), rationale (no existing test infrastructure — `package.json` `test` is a placeholder, zero test devDeps), estimated effort, and dependencies (the 9a journey list and 9b RBAC matrix from this checklist are direct inputs to the automated test epic). The `final-testing-checklist.md` MUST reference this deferral in a "Post-MVP Testing" section so a future implementer knows where to start.

CAP RATIONALE: AC9 is a single deliverable (one manual checklist doc) with multiple sections (9a–9i). The sections are NOT independently shippable features — they are sections of one document. Per the bmad-dev-auto discipline, this does NOT trigger the "multiple-goals" HALT because the goals are inseparable parts of one artifact. The doc-only nature means there is no code to break, no Appwrite infrastructure to add, and no runtime risk. The spec MUST enumerate the exact section structure of `final-testing-checklist.md` (9a–9i above) and the exact updates to `deferred-work.md` (9h AC8 expansion + 9i testing epic). If the spec derivation finds the checklist doc would exceed a reasonable size for one iteration, HALT with a proposal to split into 5.10e4a (9a–9e core AC9 sections) and 5.10e4b (9f–9i absorbed manual checks + deferral expansions) — but this is unlikely since it is a single doc.

Prerequisites confirmed done: ALL prior MVP stories in all epics (1.1–1.11, 2.1–2.9, 3.1–3.10, 4.1–4.8, 4.12–4.13, 5.1–5.4, 5.7, 5.9, 5.11–5.14) are `done` in `docs/sprint-status.yaml`, and **5.10a, 5.10b, 5.10c, 5.10d, 5.10e1, 5.10e2, and 5.10e3 are `done`** (5.10e3 done 2026-08-12, commit `cde237d`, sprint-status.yaml updated to `done` 2026-08-12 during this prompt-prep; see spec-5-10e3 Review Triage Log §2026-08-12). 5.10e4 depends on ALL prior 5.10 sub-stories — it is the final QA pass that verifies their work. No spec file exists yet for 5.10e4.

**DOC-ONLY (no code changes whatsoever — user decision 2026-08-11 + 2026-08-12):**

- NO new .vue files, NO new .js files, NO new .scss files, NO new Appwrite tables/functions/permissions/.env variables/stores/routes.
- NO modifications to any file under `src/` (the app code is FROZEN for 5.10e4 — it is a QA/documentation pass, not a code pass).
- NEW doc artifact: `docs/implementation-artifacts/final-testing-checklist.md` (the manual QA checklist — sections 9a–9i).
- MODIFIED doc artifacts: `docs/implementation-artifacts/deferred-work.md` (expand AC8 deferral entry per 9h + add post-MVP testing epic entry per 9i) and `docs/sprint-status.yaml` (finalization: flip `5-10e4` → `done`, `5-10` → `done`, `epic-5` → `done` per user decision 2026-08-12).
- NO backend changes. NO frontend changes. NO package.json changes (do NOT add Vitest/Cypress/Playwright — that is the deferred post-MVP testing epic).

**Existing app structure to document (do NOT modify code):**

5.10e4 is a documentation-only QA pass. The app (after 5.10e3) is feature-complete for MVP. The checklist MUST be derived from the ACTUAL current code state, not from planning docs — the spec MUST read the actual routes, pages, stores, permissions, seed scripts, and module structure to enumerate journeys, RBAC, and invariants. Key files to read for checklist derivation: `src/router/routes.js` (24 routes), `src/pages/` + `src/modules/*/pages/` (66 .vue page files), `src/utils/permissions.js` (canonical permission keys), `server/scripts/seed-roles.js` (role definitions), `server/scripts/setup-appwrite.js` (table list), `server/scripts/seed-*.js` (seed scripts), `package.json` (scripts), `src/stores/` (store list for caching invariants), `src/composables/` (composable list). The checklist MUST NOT reference deferred modules (5.5 Guests, 5.6 Equipment, 5.8 Energy, 4.9–4.11) as testable journeys — they are explicitly out of scope.

Continuity context from prior work (read these files before scaffolding the checklist):

- `docs/implementation-artifacts/spec-5-10e3-mobile-responsiveness.md` — 5.10e3 spec (DONE); read for the residual-risk note ("Manual visual/interactive verification at 320px was not performed") that 5.10e4's section 9f absorbs, and for the enumerated dialog/touch-target/no-wrap fix list that 9f verifies.
- `docs/implementation-artifacts/spec-5-10e2-performance-optimization.md` — 5.10e2 spec (DONE); read for the documented 3G time-to-interactive estimates (~1.95–1.97s) that 9g manually confirms, the dynamic chart.js import pattern, the caching guards (households/residents/inventory/finance), and the `fe553e9` full-page-reload-on-logout patch that 9g verifies.
- `docs/implementation-artifacts/spec-5-10e1-ux-polish-and-accessibility.md` — 5.10e1 spec (DONE); read for the page-header standardization, loading states, error handling, success notifications, and a11y fixes that the checklist's user-journey section (9a) verifies are present across pages.
- `docs/implementation-artifacts/spec-5-10c-notifications-system.md` — 5.10c spec (DONE); read for the 3 notification triggers and dedup key that 9c's data-integrity invariants (5,6,7,8) verify.
- `docs/implementation-artifacts/spec-5-10b-navigation-polish-breadcrumbs-and-quick-search.md` — 5.10b spec (DONE); read for breadcrumbs + quick-search journeys in 9a.
- `docs/implementation-artifacts/spec-5-10d-help-and-documentation.md` — 5.10d spec (DONE); read for help menu / User Guide / FAQ / tooltips journeys in 9a.
- `docs/implementation-artifacts/spec-5-10a-dashboard-completion-real-data-wiring.md` — 5.10a spec (DONE); read for the dashboard real-data wiring that 9a's dashboard journey verifies and the AC8 deferral paragraph that 9h expands.
- `docs/implementation-artifacts/spec-5-11-start-fresh-production-setup-wizard.md` — 5.11 spec (DONE); read for the Start Fresh wizard journey in 9a and the sample-data-mode lifecycle in 9e.
- `docs/implementation-artifacts/spec-5-12-user-management-crud-operations.md` — 5.12 spec (DONE); read for user CRUD + self-deactivate/last-admin guards in 9a/9c.
- `docs/implementation-artifacts/spec-5-13-role-assignment-and-permissions-management-ui.md` — 5.13 spec (DONE); read for the Roles page permission matrix in 9b.
- `docs/implementation-artifacts/spec-5-14-authentication-completeness-password-change-and-reset.md` — 5.14 spec (DONE); read for password change/reset journeys in 9a/9d.
- `docs/implementation-artifacts/deferred-work.md` — read the existing AC8 deferral entry (under "Deferred from: Story 5.10a planning (2026-08-04)") that 9h expands, and the existing "5.10e split and post-MVP testing epic" section that 9i builds on. Also read the 3 re-deferred security/realtime items to confirm they are NOT in 5.10e4's scope.
- `docs/testing.md` — the existing narrow manual testing doc (Stories 1.4 and 1.8 only); 5.10e4's checklist supersedes it for MVP sign-off but does NOT delete it (reference as a precursor).
- `docs/epics.md` — Story 5.10 AC9 (authoritative) and the full MVP story list for journey derivation.
- `docs/PRD.md` — NFR-5 (mobile 320px+ — 9f verifies), NFR-9 (performance <3s on 3G — 9g verifies), functional requirements per module.
- `docs/architecture.md` — module structure, store/composable/service layout, integration points for 9d.
- `docs/ux-specification.md` — UX flows for journey derivation in 9a.

Key design decisions for the spec to resolve:

- **Checklist doc structure:** the spec MUST define the exact section structure of `final-testing-checklist.md` matching 9a–9i, with checkbox format (`- [ ]` Markdown checkboxes) for every manual step. The doc MUST be navigable (table of contents) and reference the source spec/file for each journey so a tester can trace back.
- **Journey derivation method:** the spec MUST derive the journey list from `src/router/routes.js` + actual page files, NOT from epics.md alone (epics.md may list journeys that were re-scoped or merged). Cross-reference each journey to its owning story spec.
- **RBAC matrix format:** recommend a table (role × page/operation) with ✓/✗ cells, derived from `permissions.js` + `routes.js` `requiresPermission` + `PermissionGuard` usage. The spec MUST enumerate the exact roles (from `seed-roles.js`) and exact permission keys (from `permissions.js`).
- **Data-integrity invariant format:** each invariant gets: description, source code reference (file:function:line), manual reproduction steps, expected result, checkbox.
- **Mobile/perf manual checks:** these are absorbed from 5.10e3/5.10e2 residual risks (user decision 2026-08-12). The spec MUST include them as sections 9f and 9g with explicit Chrome DevTools mobile-emulation and 3G-throttling steps. These are the ONLY runtime checks in 5.10e4 — they are manual, not automated.
- **AC8 expansion depth:** 9h expands the deferral entry with proposed layout, server function requirements, data sources, and rough effort estimate. The spec MUST NOT implement AC8 — only document the deferral expansion. Recommend T-shirt sizing (S/M/L) for effort, not hour estimates.
- **Post-MVP testing epic scope:** 9i documents the epic scope (Vitest + Cypress/Playwright + CI), rationale (no existing infra), effort, and dependencies (9a/9b as inputs). The spec MUST NOT set up any test infrastructure — only document the deferral.
- **Sprint-status finalization:** per user decision 2026-08-12, finalization flips `5-10e4` → `done`, `5-10` → `done`, AND `epic-5` → `done`. The spec MUST include this as an explicit finalization task. The optional `epic-5-retrospective` stays `optional`.
- **No code changes:** the spec MUST NOT include any tasks that modify `src/`, `server/`, `package.json`, or any code file. If the spec derivation finds a code bug that should be fixed, it MUST be recorded as a deferred item in `deferred-work.md` (NOT fixed in 5.10e4 — this is a QA/doc pass, not a fix pass).
- **i18n / emojis:** none (hardcoded English, no emojis) — consistent with project convention. The checklist doc itself uses plain Markdown.
- `docs/implementation-artifacts/spec-5-10e3-mobile-responsiveness.md` — 5.10e3 spec (DONE); read for the residual-risk note ("Manual visual/interactive verification at 320px was not performed") that 5.10e4's section 9f absorbs, and for the enumerated dialog/touch-target/no-wrap fix list that 9f verifies.
- `docs/implementation-artifacts/spec-5-10e2-performance-optimization.md` — 5.10e2 spec (DONE); read for the documented 3G time-to-interactive estimates (~1.95–1.97s) that 9g manually confirms, the dynamic chart.js import pattern, the caching guards (households/residents/inventory/finance), and the `fe553e9` full-page-reload-on-logout patch that 9g verifies.
- `docs/implementation-artifacts/spec-5-10e1-ux-polish-and-accessibility.md` — 5.10e1 spec (DONE); read for the page-header standardization, loading states, error handling, success notifications, and a11y fixes that the checklist's user-journey section (9a) verifies are present across pages.
- `docs/implementation-artifacts/spec-5-10c-notifications-system.md` — 5.10c spec (DONE); read for the 3 notification triggers and dedup key that 9c's data-integrity invariants (5,6,7,8) verify.
- `docs/implementation-artifacts/spec-5-10b-navigation-polish-breadcrumbs-and-quick-search.md` — 5.10b spec (DONE); read for breadcrumbs + quick-search journeys in 9a.
- `docs/implementation-artifacts/spec-5-10d-help-and-documentation.md` — 5.10d spec (DONE); read for help menu / User Guide / FAQ / tooltips journeys in 9a.
- `docs/implementation-artifacts/spec-5-10a-dashboard-completion-real-data-wiring.md` — 5.10a spec (DONE); read for the dashboard real-data wiring that 9a's dashboard journey verifies and the AC8 deferral paragraph that 9h expands.
- `docs/implementation-artifacts/spec-5-11-start-fresh-production-setup-wizard.md` — 5.11 spec (DONE); read for the Start Fresh wizard journey in 9a and the sample-data-mode lifecycle in 9e.
- `docs/implementation-artifacts/spec-5-12-user-management-crud-operations.md` — 5.12 spec (DONE); read for user CRUD + self-deactivate/last-admin guards in 9a/9c.
- `docs/implementation-artifacts/spec-5-13-role-assignment-and-permissions-management-ui.md` — 5.13 spec (DONE); read for the Roles page permission matrix in 9b.
- `docs/implementation-artifacts/spec-5-14-authentication-completeness-password-change-and-reset.md` — 5.14 spec (DONE); read for password change/reset journeys in 9a/9d.
- `docs/implementation-artifacts/deferred-work.md` — read the existing AC8 deferral entry (under "Deferred from: Story 5.10a planning (2026-08-04)") that 9h expands, and the existing "5.10e split and post-MVP testing epic" section that 9i builds on. Also read the 3 re-deferred security/realtime items to confirm they are NOT in 5.10e4's scope.
- `docs/testing.md` — the existing narrow manual testing doc (Stories 1.4 and 1.8 only); 5.10e4's checklist supersedes it for MVP sign-off but does NOT delete it (reference as a precursor).
- `docs/implementation-artifacts/epic-5-context.md` — compiled epic context (reuse if valid; see step-01 rules).
- `docs/architecture.md` — module structure, store/composable/service layout, integration points for 9d.
- `docs/PRD.md` — NFR-5 (mobile 320px+ — 9f verifies), NFR-9 (performance <3s on 3G — 9g verifies), functional requirements per module for journey derivation.
- `docs/ux-specification.md` — UX flows for journey derivation in 9a.

Key design decisions for the spec to resolve:

- **Checklist doc structure:** the spec MUST define the exact section structure of `final-testing-checklist.md` matching 9a–9i, with checkbox format (`- [ ]` Markdown checkboxes) for every manual step. The doc MUST be navigable (table of contents) and reference the source spec/file for each journey so a tester can trace back.
- **Journey derivation method:** the spec MUST derive the journey list from `src/router/routes.js` + actual page files, NOT from epics.md alone (epics.md may list journeys that were re-scoped or merged). Cross-reference each journey to its owning story spec.
- **RBAC matrix format:** recommend a table (role × page/operation) with ✓/✗ cells, derived from `permissions.js` + `routes.js` `requiresPermission` + `PermissionGuard` usage. The spec MUST enumerate the exact roles (from `seed-roles.js`) and exact permission keys (from `permissions.js`).
- **Data-integrity invariant format:** each invariant gets: description, source code reference (file:function:line), manual reproduction steps, expected result, checkbox.
- **Mobile/perf manual checks:** these are absorbed from 5.10e3/5.10e2 residual risks (user decision 2026-08-12). The spec MUST include them as sections 9f and 9g with explicit Chrome DevTools mobile-emulation and 3G-throttling steps. These are the ONLY runtime checks in 5.10e4 — they are manual, not automated.
- **AC8 expansion depth:** 9h expands the deferral entry with proposed layout, server function requirements, data sources, and rough effort estimate. The spec MUST NOT implement AC8 — only document the deferral expansion. Recommend T-shirt sizing (S/M/L) for effort, not hour estimates.
- **Post-MVP testing epic scope:** 9i documents the epic scope (Vitest + Cypress/Playwright + CI), rationale (no existing infra), effort, and dependencies (9a/9b as inputs). The spec MUST NOT set up any test infrastructure — only document the deferral.
- **Sprint-status finalization:** per user decision 2026-08-12, finalization flips `5-10e4` → `done`, `5-10` → `done`, AND `epic-5` → `done`. The spec MUST include this as an explicit finalization task. The optional `epic-5-retrospective` stays `optional`.
- **No code changes:** the spec MUST NOT include any tasks that modify `src/`, `server/`, `package.json`, or any code file. If the spec derivation finds a code bug that should be fixed, it MUST be recorded as a deferred item in `deferred-work.md` (NOT fixed in 5.10e4 — this is a QA/doc pass, not a fix pass).
- **i18n / emojis:** none (hardcoded English, no emojis) — consistent with project convention. The checklist doc itself uses plain Markdown.

## Planning Artifacts to Load

Authoritative sources (load via compile-epic-context subagent for epic-5-context.md if not already compiled, plus selectively for story-specific constraints):

- docs/epics.md — Story 5.10 ACs (AC9 is the focus for 5.10e4; AC4 was 5.10e1 DONE; AC5 was 5.10e2 DONE; AC6 was 5.10e3 DONE; AC8 deferred to post-MVP) and Epic 5 story list
- docs/PRD.md — NFR-5 (mobile 320px+ — 9f verifies), NFR-9 (Performance: <3 seconds on 3G — 9g verifies), UX Design Principles, functional requirements per module for journey derivation
- docs/architecture.md — module structure, store/composable/service layout, integration points for 9d
- docs/ux-specification.md — UX flows for journey derivation in 9a
- docs/implementation-artifacts/spec-5-10e3-mobile-responsiveness.md — 5.10e3 spec (DONE); read for the residual-risk note (manual 320px verification not performed) that 9f absorbs, and the enumerated dialog/touch-target/no-wrap fix list that 9f verifies
- docs/implementation-artifacts/spec-5-10e2-performance-optimization.md — 5.10e2 spec (DONE); read for the 3G time-to-interactive estimates that 9g confirms, dynamic chart.js imports, caching guards, and `fe553e9` patch
- docs/implementation-artifacts/spec-5-10e1-ux-polish-and-accessibility.md — 5.10e1 spec (DONE); read for page-header standardization, loading states, error handling, success notifications, a11y fixes that 9a verifies
- docs/implementation-artifacts/spec-5-10b-navigation-polish-breadcrumbs-and-quick-search.md — 5.10b spec (DONE); read for breadcrumbs + quick-search journeys in 9a. NOTE the post-finalization patch `3152db1`.
- docs/implementation-artifacts/spec-5-10c-notifications-system.md — 5.10c spec (DONE); read for the 3 notification triggers + dedup key that 9c invariants verify. NOTE the post-review patch `f5e5fc9`.
- docs/implementation-artifacts/spec-5-10d-help-and-documentation.md — 5.10d spec (DONE); read for help menu / User Guide / FAQ / tooltips journeys in 9a. NOTE the post-review patch `4abd369`.
- docs/implementation-artifacts/spec-5-10a-dashboard-completion-real-data-wiring.md — 5.10a spec (DONE); read for dashboard real-data wiring (9a) and the AC8 deferral paragraph that 9h expands.
- docs/implementation-artifacts/spec-5-11-start-fresh-production-setup-wizard.md — 5.11 spec (DONE); read for Start Fresh wizard journey (9a) and sample-data lifecycle (9e).
- docs/implementation-artifacts/spec-5-12-user-management-crud-operations.md — 5.12 spec (DONE); read for user CRUD + guards in 9a/9c.
- docs/implementation-artifacts/spec-5-13-role-assignment-and-permissions-management-ui.md — 5.13 spec (DONE); read for Roles page permission matrix in 9b.
- docs/implementation-artifacts/spec-5-14-authentication-completeness-password-change-and-reset.md — 5.14 spec (DONE); read for password change/reset journeys in 9a/9d.
- docs/implementation-artifacts/epic-5-context.md — compiled epic context (reuse if valid; see step-01 rules)
- docs/implementation-artifacts/deferred-work.md — read the existing AC8 deferral entry that 9h expands, and the "5.10e split and post-MVP testing epic" section that 9i builds on. Confirm the 3 re-deferred security/realtime items are NOT in 5.10e4's scope.
- docs/testing.md — existing narrow manual testing doc (Stories 1.4 and 1.8 only); 5.10e4's checklist supersedes it for MVP sign-off but does NOT delete it.
- docs/planning-artifacts/sprint-change-proposal-2026-07-28.md — confirms 5.10's dependency on all prior MVP stories

Do NOT load POST-MVP.md as a primary source — it lists deferred modules only. Use it only to confirm a feature is deferred when in doubt.

## Project Conventions (non-negotiable)

- Frontend: Quasar v2.18.5 (Vue 3 + Vite + SSR), `<script setup>` syntax mandatory.
- Backend: Appwrite v21.2.1 (Database, Auth, Storage, Functions).
- State: Pinia. Date/Time: date-fns + date-fns-tz (village timezone from `settingsStore.timezone`, default `Africa/Lusaka`). Charts: Chart.js v4.5.1. Calendar: vue-cal v5 (`^5.0.1-rc.33`).
- Normalized ID-based relationships; composable error handling (useErrorHandler); custom form validation integrated with error handler.
- RBAC: `src/utils/permissions.js`, `src/composables/usePermissions.js` (`hasPermission('<module>:read')`, `hasPermission('<module>:write')`), route guards, PermissionGuard — reuse, do not reinvent. 5.10e4 adds NO new permission keys and changes NO route guards (it is a doc-only QA pass; the RBAC matrix in 9b DOCUMENTS the existing permissions, it does not change them).
- Dashboard widgets: follow docs/implementation-artifacts/dashboard-widget-pattern.md exactly.
- No new dependencies without verifying they're already in package.json. If a new dep is truly required, HALT with blocking condition `new dependency required: <name> — user approval needed`.
- Match existing code style in src/pages/, src/stores/, src/composables/, src/services/, src/modules/. Read neighboring modules (e.g. src/modules/school/, src/modules/farm/, src/modules/calendar/, src/modules/storage/, src/pages/setup/, src/pages/admin/) before scaffolding.
- i18n: NOT implemented in this project. vue-i18n is NOT installed. All UI strings are hardcoded English, matching existing modules (Epics 1–4 and Stories 5.1–5.14). This is a user-approved decision (2026-07-28); i18n is deferred to post-MVP — see docs/implementation-artifacts/deferred-work.md. Do NOT add vue-i18n. Do NOT use $t() or useI18n(). Write hardcoded English strings consistent with existing modules.
- No emojis in code or UI unless an existing module already uses them (do not remove existing ones).

## Strict Readiness Standard (HALT on any violation)

Before leaving step-02 (plan), the spec MUST satisfy the "Ready for Development" standard from SKILL.md:

- Actionable: every task has a file path and specific action.
- Logical: tasks ordered by dependency.
- Testable: all ACs in Given/When/Then form.
- Complete: no placeholders or TBDs.
- Sufficient: no unresolved requirement/acceptance/dependency/implementation gaps.
- Coherent: no internal contradictions.

HALT with status `blocked` and a precise blocking condition if ANY of these occur:

- A story AC cannot be translated into concrete tasks because a requirement is ambiguous or contradictory between epics.md / PRD.md / ux-specification.md / architecture.md.
- A prerequisite story is not actually `done` (verify against docs/sprint-status.yaml, not against docs/bmm-workflow-status.md which is stale).
- The working tree is dirty or the current branch is an obvious mismatch for Epic 5 work.
- A new third-party dependency is required.
- An AC requires functionality that belongs to a deferred story (5.5/5.6/5.8) or a not-yet-built story — split the work cleanly and exclude the forward-dep portion; if the AC cannot be satisfied without it, HALT.
- The intent resolves to multiple independently shippable goals that cannot be scoped into one spec — set `warnings: [multiple-goals]` in frontmatter and proceed only if they're genuinely inseparable; otherwise HALT. (5.10e4 is AC9 — a single manual checklist doc with sections 9a–9i. The sections are NOT independently shippable features — they are sections of one document, so the "multiple-goals" HALT does NOT trigger. If the spec derivation finds the checklist doc would exceed a reasonable size for one iteration, HALT with a proposal to split into 5.10e4a (9a–9e core AC9 sections) and 5.10e4b (9f–9i absorbed manual checks + deferral expansions) — but this is unlikely since it is a single doc. The broader 5.10 was split into 5.10a–5.10e per user decision 2026-08-04, and 5.10e was split into 5.10e1–5.10e4 per user decision 2026-08-11.)

Do NOT invent requirements. Do NOT pull scope from other stories into this iteration. Do NOT implement features that belong to a later story in the dependency order.

## Implementation Discipline (step-03)

- Follow the spec's task list in order. Do not reorder.
- 5.10e4 is DOC-ONLY: NO code changes to `src/`, `server/`, `package.json`, or any code file. The only files created/modified are `docs/implementation-artifacts/final-testing-checklist.md` (NEW), `docs/implementation-artifacts/deferred-work.md` (MODIFIED — 9h AC8 expansion + 9i testing epic), and `docs/sprint-status.yaml` (MODIFIED during finalization — flip 5-10e4/5-10/epic-5 to `done`).
- The checklist content MUST be derived from the ACTUAL current code state (read `routes.js`, page files, `permissions.js`, `seed-roles.js`, `setup-appwrite.js`, seed scripts, stores) — NOT from planning docs alone. Cross-reference each journey/invariant to its source file.
- Vue 3 `<script setup>` / SSR-safe / Quasar components: N/A for 5.10e4 (no code changes — these conventions apply to code, which 5.10e4 does not touch).
- Date handling: N/A for 5.10e4 (no date logic — this is a documentation pass).
- Permission checks: 5.10e4 adds NO new permission keys and changes NO route guards. The RBAC matrix in 9b DOCUMENTS the existing permissions; it does not alter them.
- No emojis in code or UI unless an existing module already uses them (do not remove existing ones). The checklist doc itself uses plain Markdown.

## Review (step-04) — Full Adversarial

Run the full adversarial review pass per the skill's step-04:

- Blind Hunter: does the implementation actually satisfy each AC as written, with no hidden gaps?
- Edge Case Hunter: walk every branching path and boundary.
- Acceptance Auditor: map each AC to concrete code/test evidence; flag any AC with no evidence.

### Review invariants for Story 5.10e4 <<< CHANGE PER ITERATION >>>

Specific invariants the review MUST verify for 5.10e4:

- **MVP user journeys checklist (9a):** `final-testing-checklist.md` enumerates every MVP user journey organized by module/feature area, with a `- [ ]` checkbox per journey; the journey list is derived from the ACTUAL `src/router/routes.js` (24 routes) and actual page files (66 .vue pages), NOT from epics.md alone; each journey cross-references its owning story spec; NO journeys for deferred modules (5.5 Guests, 5.6 Equipment, 5.8 Energy, 4.9–4.11) appear as testable journeys; the journeys cover ALL MVP modules (Residents, Households, Finance, Inventory, Farm, School, Calendar, Storage, Vendors, Module Mgmt, Dashboard, Navigation, Notifications, Help, UX polish, Performance, Mobile, Start Fresh wizard, User CRUD, Roles, Auth, Village Settings, Profile).
- **RBAC enforcement matrix (9b):** the checklist includes a role × permission × page matrix with ✓/✗ cells; the roles are derived from `server/scripts/seed-roles.js`; the permission keys are derived from `src/utils/permissions.js`; the matrix covers route guards (unauthenticated → `/auth`, missing permission → `/unauthorized`), in-page conditional rendering, the 5.13 Roles page read-only display, the 5.12 self-deactivate/last-admin guards, and the 5.9 module-toggle nav hiding; the exact permission keys (e.g. `residents:read`, `finance:write`, `*` for admin) are enumerated.
- **Data integrity validation (9c):** the checklist enumerates every cross-module data-integrity invariant (the 18 known invariants listed in 9c: finance→inventory auto-purchase, farm harvest→inventory, farm sale→finance+inventory, vendor dropdowns, notification triggers, dedup key, Start Fresh `is_using_sample_data`, sample-data wipe routing, household-before-resident ordering, soft-deactivate guards, currency/timezone formatting, module-toggle data preservation, storage quotas, personal/shared file security, full-page-reload-on-logout cache clearing); each invariant has a source code reference (file:function:line), manual reproduction steps, expected result, and checkbox; the spec verifies each invariant still holds by tracing the code path.
- **Integrations checklist (9d):** the checklist enumerates every external integration (Appwrite Database/Auth/Storage/Functions/Realtime, Chart.js, vue-cal, date-fns-tz, Pinia caching, Quasar grid) with a manual smoke step and expected result; the Appwrite Functions list includes `createAdmin`, admin-scope user creation (5.12), `createNotification` (5.10c), `storageUsageReport` (5.4).
- **Sample data mode checklist (9e):** the checklist verifies the full sample-data lifecycle (seed → banner → wipe → Start Fresh wizard → `is_using_sample_data = false` → empty-state CTAs → re-seed); the seed scripts are enumerated from `package.json` scripts and `server/scripts/seed-*.js`.
- **Manual mobile-responsiveness verification (9f):** the checklist includes a "Mobile Responsiveness (320px)" section with manual Chrome DevTools mobile-emulation steps for: the 20+ dialogs enumerated in 5.10e3 (verify no horizontal overflow, ≤95vw), dense round header/nav buttons (verify ≥44px × 44px), the 3 no-wrap rows (verify wrapping), FarmDashboardPage module cards (verify keyboard nav), MainLayout header fit at 320px, notification panel + help menu open within viewport; these are the manual checks 5.10e3 could not perform (its residual risk).
- **Manual performance spot-checks (9g):** the checklist includes a "Performance" section with manual Chrome DevTools 3G-throttling steps (verify <3s time-to-interactive on Dashboard/FinanceReportsPage/FarmDashboardPage/School dashboard), chart.js on-demand chunk loading, route lazy-loading, Pinia cache guards (no re-fetch on second load), full-page-reload-on-logout clears cached state; these confirm 5.10e2's documented estimates.
- **AC8 deferral expansion (9h):** `deferred-work.md`'s AC8 deferral entry is expanded with proposed admin page layout, server function requirements, data sources, and rough effort estimate (T-shirt sizing); AC8 is NOT implemented — only the deferral entry is expanded.
- **Post-MVP testing epic documentation (9i):** `deferred-work.md` has a new entry documenting the post-MVP testing epic (scope: Vitest + Cypress/Playwright + CI; rationale: no existing test infra; effort; dependencies: 9a/9b as inputs); `final-testing-checklist.md` references this deferral in a "Post-MVP Testing" section; NO test infrastructure is set up (no Vitest/Cypress/Playwright added to `package.json`).
- **No code changes:** NO files under `src/`, `server/`, or `package.json` were modified. Verify by checking `git diff --stat` shows only `docs/implementation-artifacts/final-testing-checklist.md` (new), `docs/implementation-artifacts/deferred-work.md` (modified), `docs/sprint-status.yaml` (modified during finalization), and the spec file itself. If any code file appears in the diff, HALT — 5.10e4 is doc-only.
- **Sprint-status finalization:** `docs/sprint-status.yaml` flips `5-10e4` → `done`, `5-10` → `done`, AND `epic-5` → `done` (per user decision 2026-08-12). The optional `epic-5-retrospective` stays `optional`. This marks 5.10 and Epic 5 as complete.
- **No deferred-scope creep:** the checklist does NOT include testable journeys or invariants for deferred modules (5.5/5.6/5.8/4.9–4.11) or deferred ACs (AC8 System Health Monitoring — only the deferral expansion in 9h). The 3 re-deferred security/realtime items from 5.10c are NOT in 5.10e4's scope.
- **`docs/testing.md` preserved:** the existing `docs/testing.md` is NOT deleted; it is referenced as a precursor in `final-testing-checklist.md`.
- If the spec set `warnings: [multiple-goals]` and HALTed to propose splitting 5.10e4 into 5.10e4a/5.10e4b, the review verifies the HALT was clean and no partial implementation was left.

**5.10 sub-story roadmap (for context — do NOT implement future sub-stories in this iteration):**

- **5.10a (DONE 2026-08-04):** Dashboard Completion — Real Data Wiring (AC1). Replaced placeholder data, verified widgets functional, <2s load.
- **5.10b (DONE 2026-08-05):** Navigation Polish — Breadcrumbs and Quick Search (AC2). Added breadcrumbs to detail/form/create/edit pages (Module → List → Detail, responsive), global header quick search with grouped results dropdown, fixed active highlighting (auto-expand nav section for active child route), clean-menu audit (removed dead `/communications` link). Post-finalization patch `3152db1` corrected a defective learner search (learners table has no name columns — reworked to search residents then resolve learner rows), the finance result field, and a header-search focus issue.
- **5.10c (DONE 2026-08-05):** Notifications System (AC3). New Appwrite `notifications` + `notification_reads` tables, `createNotification` server function (role-targeted delivery), notifications store, bell icon + count badge in MainLayout header, notification panel with filter by type and mark-as-read, Appwrite realtime live updates, and a bounded set of REAL role-targeted triggers (at-risk learner newly flagged → school roles; farm alert newly raised → `farm:read`; new vendor created → finance/farm roles). In-app only (email deferred). Post-review patch `f5e5fc9` fixed single-role users in `Query.or`. This was the only 5.10 sub-story that added new Appwrite infrastructure.
- **5.10d (DONE 2026-08-10):** Help and Documentation (AC7). Help icon in header (between bell and user profile), responsive help menu (q-menu desktop / q-dialog mobile) with User Guide / FAQ / About, new `/help` page with User Guide tab (12 sections, module-enabled-aware for Farm/School/Vendors) and FAQ tab (17 Q&A across 5 categories), and exactly 20 enumerated `q-tooltip` elements across MainLayout.vue and 9 other existing pages. Reuse-only — no new Appwrite infrastructure. Post-review patch `4abd369` fixed the help button header placement. Review Triage Log §2026-08-10 is authoritative for the current code state.
- **5.10e (split into 5.10e1–5.10e4 per user decision 2026-08-11):** UX Polish, Performance, Mobile Responsiveness, and Final Testing Checklist (AC4, AC5, AC6, AC9):
  - **5.10e1 (DONE 2026-08-11):** UX Polish and Accessibility (AC4). Consistent UI (3 heading-hierarchy fixes + post-finalization patch `26779c6` standardizing page headers across 65 pages + `6f251e7` page margins), 9 loading-state additions, 8 error-handling standardizations, 10 success-notification standardizations, 17 accessibility fixes (icon-only aria-labels, notification-badge live region, bounded `bg-warning text-dark` contrast fix), and the 2 deferred 5.10b a11y fixes (Breadcrumbs back-button destination-specific aria-label, quick-search keyboard nav via `highlightedIndex`/`@keydown` preserving `no-focus no-refocus`). Reuse-only. 1 deferred item (FarmDashboardPage clickable-card keyboard a11y → 5.10e3 per user decision 2026-08-11). `bg-info text-white` contrast deferred to post-MVP theming pass. Review Triage Log §2026-08-11 is authoritative for the current code state; `final_revision: 6f251e7`.
  - **5.10e2 (DONE 2026-08-11):** Performance Optimization (AC5). Lazy-loading audit (2 static chart.js imports → dynamic `await import()`), bundle size check (documented top 15 chunks; chart.js relocated to shared on-demand chunk), caching (bounded `loaded`/`invalidate` guards on 5 store actions across 4 files — households, residents, inventory general items, finance categories/funding sources; page/limit-keyed guards for paginated stores; CUD actions force-bypass; filter setters invalidate), removed the 300ms `setTimeout` in `useDashboardData.js`, documented 3G throttle time-to-interactive estimates (~1.95-1.97s per key page, calculated from gzip sizes). Reuse-only. Review: 0 patches, 3 deferred (low — pre-existing chart.js dynamic-import error-handling gap class shared by 9 files), 13 rejected. Post-review patch `fe553e9` (full-page-reload-on-logout to clear cached Pinia store state — fixes stale-data-across-sessions bug class for ALL cached stores, surfaced during Epic 5 review preceding 5.10e3). The 5.10e2 spec's `baseline_revision` is `0f9458e`; its Review Triage Log §2026-08-11 is authoritative for the current code state. Dep: 5.10e1. DONE 2026-08-11.
  - **5.10e3 (DONE 2026-08-12):** Mobile Responsiveness (AC6). Full 320px audit of ALL 66 pages + all fixes (global CSS in `app.scss` + targeted per-file fixes for scoped-style dialogs), 44px touch-target audit (per user decision 2026-08-11: full audit + all fixes; global CSS for dense round buttons + per-file for `size="xs"`), FarmDashboardPage clickable-card keyboard a11y (per user decision 2026-08-11: folded into 5.10e3), no-wrap flex row fixes, table horizontal-scroll audit. Reuse-only. Commit `cde237d`; 1 low patch (CSS var fallback for focus outline), 15 rejected findings. Residual risk: manual 320px visual/interactive verification NOT performed (only `npm run build`) — absorbed into 5.10e4's section 9f per user decision 2026-08-12.
  - **5.10e4 (THIS ITERATION — FINAL Epic 5 story):** Final Testing Checklist (AC9). Manual final-testing-checklist.md doc covering all MVP user journeys, RBAC matrix, data integrity, integrations, sample-data mode (per user decision 2026-08-11: manual checklist only; automated test infrastructure deferred to a new post-MVP testing epic — 5.10e4 documents the new epic in deferred-work.md). Per user decision 2026-08-12, 5.10e4 ALSO absorbs the manual 320px mobile checks (9f) and manual 3G performance spot-checks (9g) from 5.10e3/5.10e2 residual risks, expands the AC8 deferral entry (9h), and documents the post-MVP testing epic (9i). DOC-ONLY — no code changes. Finalization flips `5-10e4`, `5-10`, AND `epic-5` to `done` in sprint-status.yaml (per user decision 2026-08-12). Completing 5.10e4 marks 5.10 and Epic 5 as done.
- **AC8 (System Health Monitoring): DEFERRED to post-MVP** (user decision 2026-08-04). Documented in `deferred-work.md` during 5.10a; 5.10e4 expands the deferral entry (9h: proposed admin page layout, server function requirements, data sources, estimated post-MVP effort). Not implemented in MVP.

**Prior Epic 5 story summaries (for regression-checking context):**

**5.9 (Module Management):** Admin page at `/admin/modules`. Core modules always enabled (Residents, Households, Finance, Inventory, Calendar, Storage). Optional MVP modules toggleable: Farm, School, Vendors ONLY (NOT Guests/Equipment/Energy — deferred). Toggle hides nav/widgets but preserves data. Dependency warning on disable. Updates `settingsStore.modulesEnabled`. Dep: all MVP previous stories.

**5.14 (Auth Completeness):** ProfilePage "Change Password" dialog. AuthPage "Forgot password?" link → `Account.createRecovery` → email link → `/auth/reset-password` page → `Account.updateRecovery`. Email verification deferred. No self-service signup. Deps: 1.3, 1.11.

**5.12 (User CRUD):** UsersPage `/admin/users` "Add User" button (System Admin only). Server-side Appwrite Function for admin-scope user creation. Soft-deactivate. Cannot deactivate self or last System Administrator. Audit logging. Deps: 1.4, 1.11.

**5.13 (Role Assignment UI):** UsersPage "Manage Roles" dialog. "View Permissions" shows effective permission union. `/admin/roles` page: role list with permission matrix. Read-only for MVP. `seed-roles.js` upsert-capable. Dep: 5.12.

**5.11 (Start Fresh Wizard):** SetupWizard "Start Fresh" card enabled. 5-step wizard: Village Profile → Admin User → Village Head → Module Selection → First Household. Sets `is_using_sample_data = false`. Empty-state CTAs on dashboard and list pages. Last-System-Admin guard added to `updateUser`. Deps: 5.9, 5.12. DONE 2026-08-04.

**5.10a (Dashboard Completion — Real Data Wiring):** Replaced `DashboardPage.vue` placeholder data with real, permission-gated Appwrite fetches via new `src/composables/useDashboardData.js` (household/resident counts via `Query.limit(1)` + `.total`; finance totals via `financeStore.fetchSummary()`; recent activity via per-module `tables.listRows` with `Query.orderDesc('$createdAt')` + `Query.limit(3)`, merged/sorted/sliced to 8). Extended `QuickStatsWidget` with Total Income/Total Expense cards. Each module fetch isolated (try/catch) so one failure doesn't break the dashboard. SSR-safe (onMounted only). AC8 (System Health Monitoring) deferred to post-MVP and documented in `deferred-work.md`. NOTE: a 300ms `setTimeout` hydration delay remains in `useDashboardData.load()` — owned by 5.10e2 (performance) for removal. Dep: all prior MVP stories. DONE 2026-08-04.

**5.10b (Navigation Polish — Breadcrumbs and Quick Search):** Added reusable `src/components/layout/Breadcrumbs.vue` (responsive Module → List → Detail, mobile back-button-only) replacing ad-hoc Back buttons on 22 detail/form/create/edit pages; added `meta.breadcrumb` to existing routes; added `src/composables/useGlobalSearch.js` (permission-gated, SSR-safe quick search across households/residents/finance/plots/learners/vendors/inventory/calendar + nav items, grouped dropdown); added a route watcher in `MainLayout.vue` to auto-expand the active `expandedSections` key; removed the dead `/communications` nav link. Post-finalization patch `3152db1` (2026-08-05) corrected a defective learner search (the `learners` table has NO `first_name`/`last_name` columns — it joins `residents` via `resident_id`; reworked to search residents first then resolve learner rows), the finance result secondary field (`amount_funded ?? amount` + ZMW currency + date), and a header-search focus issue (`no-focus no-refocus` on the results `q-menu`). The 5.10b spec's `final_revision` is `3152db1`; its Review Triage Log §2026-08-05 is authoritative for the current code state. Dep: 5.10a. DONE 2026-08-05.

**5.10c (Notifications System):** Added new Appwrite `notifications` table (role-targeted, one row per event) and `notification_reads` table (per-user read receipts, row-secured matching the `file_metadata` precedent). New `createNotification` Appwrite Function derives `target_roles` from a hardcoded `TYPE_CONFIG` authorization+targeting matrix (never trusts client-supplied targeting; validates caller permission per type). New `src/stores/notifications-store.js` Pinia store (fetchMyNotifications with role-intersection query + read-receipt join, unreadCount getter, markRead/markAllRead, filterByType, createNotification helper). Bell `q-btn` + `q-badge` + `NotificationPanel.vue` in `MainLayout.vue` header (q-menu desktop, q-dialog mobile). Appwrite realtime subscription with 30s polling fallback. Exactly 3 triggers: at-risk learner newly flagged (school roles, dedup by type+related_entity_type+related_entity_id), farm alert newly raised (farm roles, same dedup), new vendor created (finance/farm roles). Seeded demo notifications in `seedAllData`. Review: 2 patches (1 high — deterministic SHA-256 rowId for race-free dedup; 1 low — missing `delete` permission on `notification_reads`), 3 deferred items (table-level create griefing vector, realtime sync-only error handling, at-risk 60s cache re-flip suppression — all pre-existing-pattern-class; per user decision 2026-08-11 these 3 security/realtime items are re-deferred to post-MVP hardening, NOT owned by any 5.10e sub-story), 7 rejected findings. Post-review patch `f5e5fc9` fixed single-role users in `Query.or` (used `Query.contains` for single role, `Query.or`+`Query.contains` for multiple). Dep: 5.10b. DONE 2026-08-05.

**5.10d (Help and Documentation):** Added a help `q-btn` (`icon="help"`) in `MainLayout.vue` header between the 5.10c notifications bell and the user profile `q-btn`, with a responsive help menu (`q-menu` desktop / `q-dialog` mobile) containing User Guide / FAQ / About (version + village name) links — shared via a new `src/components/layout/HelpMenuList.vue` component (extracted during review patch to deduplicate the menu/dialog markup, mirroring the `NotificationPanel.vue` reuse pattern). Added a new `/help` route (`requiresAuth: true`, no `requiresPermission`, no breadcrumb — top-level page like Dashboard) and a new `src/pages/help/HelpPage.vue` with two `q-tab-panels`: "User Guide" (Getting Started card + 11 `q-expansion-item` module sections, module-enabled-aware for Farm/School/Vendors showing a "not enabled" banner post-hydration) and "FAQ" (17 Q&A across 5 categories: General, Data Entry, Roles & Permissions, Troubleshooting, Sample Data vs Real Data). Added exactly 20 enumerated `q-tooltip` elements: help icon, notifications bell, quick-search input, 7 nav-section headers in `MainLayout.vue`, 7 primary action buttons across 6 list pages, and 3 complex form fields (VillageSettingsPage currency, StorageSettingsPage quota, LongTermGoalsSettingsPage benchmark threshold — substituted for the non-existent at-risk attendance threshold UI control). Reuse-only — no new Appwrite tables, functions, permissions, `.env` vars, or stores. Review: 1 patch (low — `HelpMenuList.vue` extraction), 16 rejected findings (all out-of-scope or factually incorrect), 0 deferred. Post-review patch `4abd369` fixed the help button header placement. The 5.10d spec's `baseline_revision` is `d6e8be0`; its Review Triage Log §2026-08-10 is authoritative for the current code state. Dep: 5.10c. DONE 2026-08-10.

**5.10e1 (UX Polish and Accessibility):** Audit-and-fix pass delivering AC4. 4a Consistent UI: 3 heading-hierarchy fixes (HouseholdsListPage/FinanceReportsPage → `text-h5`, DashboardPage `text-h4` → `text-h5`); post-finalization patch `26779c6` extended this to ALL 65 pages (standardized `<h4 class="text-h5 q-my-none">` + `<p class="text-grey-7 q-mb-none">` subtitle), plus `6f251e7` page-margin alignment on 9 pages. 4b Loading states: 9 pages got loading indicators (InventoryListPage, FarmDashboardPage, CropsListPage, PlantingsListPage, SalesListPage, ClassesListPage, VendorsListPage, AllLoansPage; CalendarPage already covered). 4c Error handling: 8 standardizations (DashboardPage + 10 useDashboardData catch blocks got non-blocking `notifyError`; farm-store 6 catch blocks got `notifyError`; CreatePlantingPage error → `handleError`; VillageSettingsPage 5 catches → `notifyError`; MainLayout realtime-subscribe-failure notified once; lendingStore + notifications-store de-duplicated redundant `console.error`). 4d Success confirmations: 10 CUD actions standardized to `useErrorHandler().notifySuccess()` (PlotFormPage create/update, PlotsListPage delete, CropFormPage create, PlantingDetailPage harvest start/complete, CreatePlantingPage record, InventoryFormPage create/update, InventoryDetailPage sale, MainLayout logout, FinanceDashboardPage export, FinanceTransactionsPage funding add, VillageSettingsPage council member save/remove). 4e Accessibility: 17 fixes (5 icon-only aria-labels each on HouseholdsListPage + ResidentsListPage, 3 on PlotsListPage, 2 on LearnersListPage, 1 on LongTermGoalsSettingsPage back button, 1 on FinanceTransactionsPage inventory-link, 3 refresh-button aria-labels on farm widgets, notification-badge `role="status"` + `aria-live="polite"`, 6 `bg-warning text-white` → `text-dark` contrast fixes). 4f Deferred a11y: Breadcrumbs.vue `backAriaLabel` computed (destination-specific, derived from `props.items`); MainLayout quick-search keyboard nav via `highlightedIndex` ref + `flattenedResults` computed + `@keydown` handler (ArrowDown/ArrowUp/Enter/Escape) preserving `no-focus no-refocus`. Reuse-only — no new Appwrite infra. Review: 1 patch (medium — malformed `deferred-work.md` entry corrected), 1 deferred (medium — FarmDashboardPage clickable-card keyboard a11y → 5.10e3 per user decision 2026-08-11), 19 rejected. `bg-info text-white` contrast (~1.8:1, 10+ files) deferred to post-MVP theming pass. The 5.10e1 spec's `baseline_revision` is `2acced5`; `final_revision` is `6f251e7`; its Review Triage Log §2026-08-11 is authoritative for the current code state. Dep: 5.10d. DONE 2026-08-11.

**5.10e2 (Performance Optimization):** Audit-and-fix pass delivering AC5. 5a Lazy-loading: converted the 2 remaining static chart.js imports to dynamic `await import()` — `FinanceReportsPage.vue` (static `import { Chart, registerables } from 'chart.js'` + module-scope `Chart.register(...)` → cached async `ensureChart()` helper invoked from `createChart()`) and `InventoryStockWidget.vue` (static `import Chart from 'chart.js/auto'` → dynamic import inside `renderChart()`, now `async`). Verified all 24 routes in `routes.js` still use `() => import(...)` (no static route imports introduced). 5b Bundle size: documented top 15 chunks from `npm run build` (jspdf 390KB, chart 198KB, html2canvas 195KB, index.es 148KB, index 103KB, jszip 94KB, appwrite 83KB, vue-cal 74KB, QIcon 66KB, MainLayout 54KB, farm-store 45KB, FarmDashboardPage 39KB, PlantingDetailPage 37KB, DashboardPage 35KB, FinanceReportsPage 28KB). Post-fix build confirms chart.js is no longer in the FinanceReportsPage or dashboard chunks — it's a shared on-demand `chart-*.js` chunk (198KB). No chunk >250KB on a common navigation path (jspdf/html2canvas are on-demand export-only). 5c Removed the 300ms `setTimeout` hydration delay in `useDashboardData.js:310-311` (introduced by 5.10a as a hydration-safety workaround; unnecessary since fetches run in `onMounted` which is client-only). 5d Caching: added `loaded`-flag cache guards to 5 store actions across 4 files — `households-store.js` (`fetchHouseholds` with page/limit-keyed guard), `residents-store.js` (`fetchResidents` with page/limit-keyed guard; `setSearchFilter`/`setHouseholdFilter`/`clearFilters` invalidate), `inventory-store.js` (`fetchItems` with page/limit-keyed guard; `setFilters`/`clearFilters` invalidate), `finance-store.js` (added read-guard to `fetchCategories`/`fetchFundingSources` using pre-existing `categoriesLoaded`/`fundingSourcesLoaded` flags; `fetchDashboardData` forwards `forceRefresh`). CUD actions force-bypass the cache (`force=true` 3rd arg). Verified `vendors-store`, `farm-store` (plots/crops), `class-store`, `learner-store`, `calendar-store` already cache correctly — left untouched. `notifications-store`/`auth-store`/`settings-store` excluded from caching. No `useStoreCache.js` helper extracted (caching patterns differ between paginated and non-paginated stores). 5e 3G test: documented Slow 3G throttle procedure and calculated time-to-interactive estimates for 6 key pages (~1.95-1.97s each, under the 3s AC5 budget) using gzip sizes from the post-fix build report (methodology: byte-budget calculation, not a live DevTools recording — follow-up manual spot-check recommended for 5.10e4's final testing checklist). Reuse-only — no new Appwrite infrastructure. Review: 0 patches, 3 deferred (low — pre-existing chart.js dynamic-import error-handling gap class shared by 9 files, not caused by this story), 13 rejected (mix of factually-incorrect claims and correct-behavior-mistaken-for-bugs). Post-review patch `fe553e9` (2026-08-11, applied during Epic 5 review preceding 5.10e3): changed `MainLayout.handleLogout()` from `router.push('/auth')` to `window.location.href = '/auth'` (full page reload) to clear all Pinia store state on logout — fixes a stale-data-across-sessions bug class where cached `loaded` flags from User A's session would serve User A's data to User B after login on a shared device. This patch fixes ALL cached stores (5.10e2 additions + pre-existing vendors/farm/calendar/class/learner), not just the 5.10e2 additions. The 5.10e2 spec's `baseline_revision` is `0f9458e`; its Review Triage Log §2026-08-11 is authoritative for the current code state. Dep: 5.10e1. DONE 2026-08-11.
