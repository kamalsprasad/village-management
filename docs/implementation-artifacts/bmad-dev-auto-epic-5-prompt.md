# /bmad-dev-auto Prompt — Epic 5 Final Story (5.10 Sub-Stories: 5.10a → 5.10e, with 5.10e split into 5.10e1 → 5.10e4)

> **Usage:** Copy everything below the `---` line into a fresh `/bmad-dev-auto` invocation.
> **Adapting for subsequent iterations:** Change ONLY the three sections marked
> `<<< CHANGE PER ITERATION >>>` — (1) Current Iteration Target, (2) Story X.Y Specifics,
> (3) Review invariants. Everything else is campaign-level context that stays constant.
> The MVP dependency order in "Epic 5 MVP Scope" tells you which sub-story is next after
> each completion.
>
> **Sub-story split (user decision 2026-08-04):** Story 5.10 "System Completion" has 9 broad
> ACs spanning 6+ independently shippable features. Per the bmad-dev-auto discipline rules
> ("HALT if the intent resolves to multiple independently shippable goals"), 5.10 is split
> into sub-stories 5.10a–5.10e, each processed in its own iteration. AC8 (System Health
> Monitoring) is deferred to post-MVP with thorough documentation (user decision 2026-08-04).
> AC3 (Notifications) is included in MVP as sub-story 5.10c (user decision 2026-08-04).
>
> **5.10e sub-split (user decision 2026-08-11):** 5.10e itself covers 4 broad ACs
> (AC4 UX polish/accessibility, AC5 performance, AC6 mobile responsiveness, AC9 final
> testing checklist) — the same multi-goal pattern that caused 5.10 to be split. Per the
> bmad-dev-auto discipline, 5.10e is pre-split into sub-stories 5.10e1–5.10e4, each
> processed in its own iteration. AC9's automated test infrastructure is deferred to a new
> post-MVP testing epic (user decision 2026-08-11); 5.10e4 produces only a manual final
> testing checklist doc for MVP. Completing 5.10e4 marks 5.10 and Epic 5 as done.

---

You are running bmad-dev-auto for the Sustainable Model Village Management System (village-app). This invocation processes EXACTLY ONE story this iteration and HALT cleanly so the next iteration can pick up the next story.

## Current Iteration Target <<< CHANGE PER ITERATION >>>

Epic: 5 — "Village Calendar, Storage, Optional Modules, and User Management"
Story to implement THIS iteration: 5.10e2 — Performance Optimization (AC5)
Epic context file to load/compile: {implementation_artifacts}/epic-5-context.md
Spec file to produce: {implementation_artifacts}/spec-5-10e2-performance-optimization.md

If the spec file already exists with status `draft`, resume it. If it exists with any other status, do NOT overwrite — HALT with blocking condition `spec already in progress/done; user decision required`.

## Epic 5 MVP Scope (do NOT implement deferred stories)

MVP stories, in dependency order (stories marked ✅ are done; ← THIS ITERATION marks the current target):

1. ✅ 5.1 Village Calendar - Global Calendar with Category Filtering (deps: Epic 1) — DONE 2026-07-28
2. ✅ 5.2 Village Calendar - Role-Based Event Creation and Editing (deps: 5.1) — DONE 2026-07-29
3. ✅ 5.3 Cloud Storage - Role-Based Storage Quotas and Personal Folders (deps: 1.10) — DONE 2026-07-30
4. ✅ 5.4 Cloud Storage - Shared Folders and Module-Based Access (deps: 5.3) — DONE 2026-07-31
5. ✅ 5.7 Vendors/Suppliers Management Module (deps: 2.2, 2.3, 3.8 — all done) — DONE 2026-08-01
6. ✅ 5.9 Module Management and Configuration (deps: all MVP previous) — DONE 2026-08-03
7. ✅ 5.14 Authentication Completeness - Password Change and Reset (deps: 1.3, 1.11) — DONE 2026-08-03
8. ✅ 5.12 User Management - CRUD Operations (deps: 1.4, 1.11) — DONE 2026-08-03
9. ✅ 5.13 Role Assignment and Permissions Management UI (deps: 5.12) — DONE 2026-08-04
10. ✅ 5.11 Start Fresh Production Setup Wizard (deps: 5.9, 5.12) — DONE 2026-08-04
11. 5.10 System Completion — split into sub-stories (user decision 2026-08-04; 5.10e re-split 2026-08-11):
    - ✅ 5.10a Dashboard Completion — Real Data Wiring and Widget Finalization (AC1) — DONE 2026-08-04
    - ✅ 5.10b Navigation Polish — Breadcrumbs and Quick Search (AC2) — DONE 2026-08-05 (post-finalization patch `3152db1` applied; see spec-5-10b Review Triage Log §2026-08-05)
    - ✅ 5.10c Notifications System (AC3) — includes new Appwrite `notifications` table — DONE 2026-08-05 (post-review patch `f5e5fc9` applied; see spec-5-10c Review Triage Log §2026-08-05)
    - ✅ 5.10d Help and Documentation (AC7) — DONE 2026-08-10 (post-review patch `4abd369` applied; see spec-5-10d Review Triage Log §2026-08-10)
    - 5.10e UX Polish, Performance, Mobile Responsiveness, and Final Testing Checklist — split into sub-stories (user decision 2026-08-11):
      - ✅ 5.10e1 UX Polish and Accessibility (AC4) — DONE 2026-08-11 (post-finalization patches `26779c6` page-header standardization + `6f251e7` page margins applied; see spec-5-10e1 Review Triage Log §2026-08-11)
      - 5.10e2 Performance Optimization (AC5) ← THIS ITERATION
      - 5.10e3 Mobile Responsiveness (AC6)
      - 5.10e4 Final Testing Checklist (AC9 — manual doc; automated test infra deferred to new post-MVP testing epic)
    - AC8 System Health Monitoring — DEFERRED to post-MVP (user decision 2026-08-04; documented in deferred-work.md during 5.10a; 5.10e4 will verify/expand the deferral entry)

DEFERRED (post-MVP — out of scope, do NOT implement, do NOT add toggles for them): 5.5 Guests, 5.6 Equipment, 5.8 Energy, 4.9–4.11, 5.10 AC8 (System Health Monitoring).

## Story 5.10e2 Specifics <<< CHANGE PER ITERATION >>>

Intent: Deliver the Performance Optimization sub-story for MVP Story 5.10. Implement Story 5.10 AC5: "Performance optimization: <3 seconds on 3G, lazy loading, caching." This sub-story is an AUDIT + BOUNDED-FIX pass across the existing MVP frontend — it audits lazy-loading coverage (route-level and component-level dynamic imports for heavy vendor libs), bundle size (document current chunk sizes, identify optimization opportunities), removes the 300ms `setTimeout` hydration delay in `useDashboardData.js` (owned by 5.10e2 since 5.10a), audits store-level caching (most Pinia stores re-fetch on every page mount with no `loaded`/`lastFetch` guard — add bounded caching to high-traffic stores), and documents the 3G manual throttle test procedure and results. This is a REUSE-ONLY story — NO new Appwrite tables, server functions, permissions, `.env` variables, stores, pages, or routes. It MODIFIES existing composables, stores, and component import patterns for performance. It MAY add a small shared caching helper composable IF the caching pattern is repeated across many stores — but the spec MUST first verify no such helper exists and justify any extraction. This is Story 5.10 AC5 only — AC4 (UX polish/accessibility) was 5.10e1 (DONE), AC6 (mobile responsiveness) is 5.10e3, AC9 (final testing checklist) is 5.10e4. AC8 (System Health Monitoring) is deferred to post-MVP.

Like 5.10e1, 5.10e2 is REUSE-ONLY: it adds NO new Appwrite tables, NO new server functions, NO new permissions, NO new `.env` variables, NO new stores, NO new pages, and NO new routes. It MODIFIES a bounded set of existing composables, stores, and component import statements for performance. It REUSES the existing Pinia store structure, the existing `() => import()` route-level lazy-loading pattern, the existing `await import()` dynamic-import pattern already used for chart.js/jspdf in most files, and the existing `loaded`/`isLoading` ref patterns where they exist. No existing module server functions, Appwrite tables, permissions, or route definitions are touched.

ACs (from docs/epics.md Story 5.10 AC5 — treat as authoritative):

5. Performance optimization: <3 seconds on 3G, lazy loading, caching.

Sub-ACs for 5.10e2 (derived from AC5):

5a. **Lazy-loading audit.** Audit lazy-loading coverage across the app. Route-level: ALL 24 route components in `src/router/routes.js` already use `() => import(...)` — verify this is still the case and no static route imports were introduced. Component-level heavy vendor libs: audit static vs dynamic imports of `chart.js`, `jspdf`, `jspdf-autotable`, `html2canvas`, `jszip`, `vue-cal`. The spec MUST grep for static imports of these libs and enumerate any that should be converted to `await import()` (dynamic). Known findings to verify: (1) `src/modules/finance/pages/FinanceReportsPage.vue:755` has a STATIC `import { Chart, registerables } from 'chart.js'` — all other chart.js usages (FarmReportsPage, PlotDetailPage, EducationalGoalsPage, YieldTrendsWidget, PlotsOverviewWidget, IncomeExpenseTrendWidget, TopExpenseCategoriesWidget) already use `await import('chart.js')` dynamically; (2) `src/components/dashboard/InventoryStockWidget.vue:111` has a STATIC `import Chart from 'chart.js/auto'` — this pulls chart.js into the dashboard's chunk even though the widget may not render immediately. The spec MUST convert these 2 static imports to dynamic `await import()` matching the established pattern, and verify no other static heavy-lib imports exist. CAP at 5-8 import conversions.

5b. **Bundle size check.** Run `npm run build` and document the current chunk sizes (from `dist/spa/assets/*.js`). The spec MUST enumerate the top 10-15 chunks by size. Known current sizes (as of 5.10e1 done, build 2026-08-11): jspdf 390KB, chart.js 198KB, html2canvas 195KB, index.es 148KB, index 103KB, jszip 94KB, appwrite 83KB, vue-cal 74KB, QIcon 66KB, MainLayout 54KB, farm-store 45KB, FarmDashboardPage 39KB, PlantingDetailPage 37KB, DashboardPage 35KB. The spec MUST verify whether the 5a lazy-loading conversions reduce any chunk sizes (e.g., chart.js should no longer be in the FinanceReportsPage or dashboard chunks after dynamic conversion — it becomes a shared chunk loaded on demand). Document whether any single chunk exceeds a reasonable threshold (recommend flagging any chunk >250KB that loads on a common navigation path, not just on-demand export). If a chunk is too large and cannot be reduced without a new dependency or architectural change, document it as a deferred item with rationale.

5c. **Remove 300ms `setTimeout` in `useDashboardData.js`.** The `load()` function in `src/composables/useDashboardData.js` has a 300ms `setTimeout` hydration delay (line ~311: `await new Promise((resolve) => setTimeout(resolve, 300))`) introduced during 5.10a as a hydration-safety workaround. 5.10e2 MUST remove this delay and verify the dashboard still hydrates correctly (SSR-safe — the fetches are already in `onMounted`, which only runs client-side, so the delay is unnecessary). The spec MUST verify no hydration mismatch or race condition occurs after removal (test the dashboard with real data after the change). This is a single, specific fix — no cap needed.

5d. **Caching audit.** Audit Pinia stores for caching: most stores re-fetch from Appwrite on every page mount with no `loaded`/`lastFetch`/`cache` guard, causing redundant network calls when a user navigates back to a list page they already visited. The spec MUST grep all stores in `src/stores/` and `src/modules/*/stores/` for existing caching patterns (`loaded`, `lastFetch`, `fetchedAt`, `cache`), identify which high-traffic stores lack caching, and add bounded caching (a `loaded` boolean + skip-re-fetch-if-loaded pattern, matching the existing `inventory-store.js` `farmInputsLoaded` pattern or the `calendar-store.js` `loaded` pattern) to a CAPPED set of high-traffic stores. Prioritize: `households-store`, `residents-store`, `inventory-store` (general items, not just farm inputs), `finance-store` (categories/funding sources), `farm-store` (plots/crops), `school-store` or equivalent (classes/learners), `vendors-store`. CAP at 6-8 stores to keep within one iteration. The caching MUST be invalidateable (a `refresh()` method or a `invalidate()` call that clears `loaded` and re-fetches) so CUD actions can bust the cache after writes. The spec MUST enumerate which stores get caching, the exact cache-invalidation points (which CUD actions call `invalidate`), and verify no stale-data bugs are introduced (e.g., after creating a household, the list must show the new household — the create action must bust the cache or append to the cached list). If the caching pattern is repeated across >6 stores identically, the spec MAY extract a small `useStoreCache()` helper composable — but MUST first verify no such helper exists in `src/composables/` and justify the extraction.

5e. **3G manual throttle test documentation.** AC5 requires "<3 seconds on 3G." The spec MUST document the manual 3G throttle test procedure (Chrome DevTools → Network → "Slow 3G" preset → load each key page → measure time-to-interactive) and record the results for the highest-traffic pages (Dashboard, Households list, Residents list, Finance transactions, Farm dashboard, School dashboard). This is a DOCUMENTATION task — no code change for 5e, just a test-procedure + results section in the spec (and optionally a `docs/implementation-artifacts/performance-test-results.md` doc). If any page exceeds 3s on Slow 3G, the spec MUST flag it and propose a targeted optimization (or defer it with rationale if the fix is out-of-scope for 5.10e2's cap).

CAP RATIONALE: AC5 spans lazy-loading, bundle size, the setTimeout removal, caching, and 3G testing — each sub-AC (5a-5e) caps its fix set to keep within one iteration. The spec MUST enumerate the exact fix set per sub-AC (file:line + fix description) — do NOT leave the fix set as TBD. If the total fix count across all sub-ACs exceeds one iteration, set `warnings: [multiple-goals]` and HALT with a proposal to split 5.10e2 into 5.10e2a (lazy-loading + bundle size + setTimeout — 5a-5c) and 5.10e2b (caching + 3G test — 5d-5e) — per the bmad-dev-auto discipline. Do NOT silently expand scope.

Prerequisites confirmed done: ALL prior MVP stories in all epics (1.1–1.11, 2.1–2.9, 3.1–3.10, 4.1–4.8, 4.12–4.13, 5.1–5.4, 5.7, 5.9, 5.11–5.14) are `done` in `docs/sprint-status.yaml`, and **5.10a, 5.10b, 5.10c, 5.10d, and 5.10e1 are `done`** (5.10e1 done 2026-08-11 after post-finalization patches `26779c6` + `6f251e7`; see spec-5-10e1 Review Triage Log §2026-08-11). 5.10e2 depends on 5.10e1 — it audits the pages/components/composables that 5.10a–5.10e1 built (including the 5.10e1 standardized loading states, error handling, and the 5.10a `useDashboardData.js` with the 300ms setTimeout that 5.10e2 removes). No spec file exists yet for 5.10e2.

**REUSE-ONLY (no new Appwrite infrastructure — user decision 2026-08-11):**

- NO new tables, NO new server functions, NO new permissions, NO new `.env` variables, NO new stores, NO new pages, NO new routes.
- MODIFIED frontend artifacts: a bounded set of existing composables (`useDashboardData.js`), stores (`src/stores/`, `src/modules/*/stores/`), and component import statements (static → dynamic for heavy vendor libs) — the spec enumerates exact files per sub-AC (5a-5e) with file:line + fix.
- NEW frontend artifacts (only if justified per 5d): OPTIONALLY a shared `src/composables/useStoreCache.js` helper IF the spec verifies no equivalent helper exists AND the caching pattern is repeated across >6 stores identically. The spec MUST justify any new composable; default is in-place caching additions to each store.
- OPTIONALLY a new `docs/implementation-artifacts/performance-test-results.md` doc for the 3G throttle test results (5e).
- NO backend changes whatsoever.

**Existing app structure to audit (do NOT break functionality):**

5.10e2 is a performance-audit-and-fix pass across the existing MVP frontend. The app (after 5.10e1) renders via `MainLayout.vue` with route-level lazy-loading already in place for all 24 routes. Heavy vendor libs (chart.js, jspdf, html2canvas, jszip, vue-cal) are mostly dynamically imported except 2 known static chart.js imports (FinanceReportsPage.vue:755, InventoryStockWidget.vue:111). Pinia stores mostly re-fetch on every page mount without caching. The dashboard has a 300ms artificial delay in `useDashboardData.js`. 5.10e2 MUST NOT break any page's functionality — it only changes import strategies (static → dynamic), removes the artificial delay, and adds caching guards. The 5.10b quick-search, 5.10c bell + badge + panel + realtime subscription, 5.10d help icon + menu + tooltips, 5.10e1 loading states + error handling + a11y fixes, `expandedSections` watcher, and all module CRUD flows must continue to work after the performance pass. Caching additions MUST NOT cause stale-data bugs — every CUD action must bust the relevant cache.

Continuity context from prior work (read these files before scaffolding):

- `src/composables/useDashboardData.js` — the 5.10a dashboard data composable. 5.10e2 removes the 300ms `setTimeout` at ~line 311 and verifies the dashboard still hydrates correctly. Read the full file before editing.
- `src/router/routes.js` — the route definitions. 5.10e2 verifies all 24 routes still use `() => import(...)` (no static route imports introduced since 5.10e1). Read to confirm lazy-loading coverage.
- `src/stores/` and `src/modules/*/stores/` — all Pinia stores. 5.10e2 audits caching patterns and adds bounded caching to high-traffic stores. Read `inventory-store.js` (has `farmInputsLoaded` pattern) and `calendar-store.js` (has `loaded` pattern) as reference for the caching approach. Grep all stores for `loaded`/`lastFetch`/`fetchedAt`/`cache` to identify which already cache and which don't.
- `src/modules/finance/pages/FinanceReportsPage.vue:755` and `src/components/dashboard/InventoryStockWidget.vue:111` — the 2 known static chart.js imports. 5.10e2 converts these to dynamic `await import('chart.js')` matching the pattern in `FarmReportsPage.vue:933`, `PlotDetailPage.vue:558`, etc.
- `docs/implementation-artifacts/spec-5-10e1-ux-polish-and-accessibility.md` — 5.10e1 spec (DONE); read to confirm 5.10e1's loading-state and error-handling fixes are in place so 5.10e2's caching changes don't conflict with them (e.g., a cached store should still show a loading state on first load, just skip re-fetch on subsequent loads). NOTE the post-finalization patches `26779c6` (page-header standardization) and `6f251e7` (page margins).
- `docs/implementation-artifacts/spec-5-10a-dashboard-completion-real-data-wiring.md` — 5.10a spec (DONE); read for the `useDashboardData.js` context and the 300ms setTimeout's origin (5.10a introduced it as a hydration-safety workaround; 5.10e2 removes it).
- `docs/implementation-artifacts/spec-5-10b-navigation-polish-breadcrumbs-and-quick-search.md` — 5.10b spec (DONE); read for the quick-search and breadcrumbs context (5.10e2 must not break these).
- `docs/implementation-artifacts/spec-5-10c-notifications-system.md` — 5.10c spec (DONE); read for the notifications store context (5.10e2's caching audit may touch `notifications-store.js` — but notifications should NOT be cached long-term since they need freshness for the unread badge; the spec MUST exclude notifications-store from caching or use a very short TTL).
- `docs/implementation-artifacts/deferred-work.md` — carry-forward items; the 300ms setTimeout is owned by 5.10e2 (this story). The 3 security/realtime items (notification_reads griefing, realtime async-failure, at-risk cache re-flip) are re-deferred to post-MVP per user decision 2026-08-11 — 5.10e2 does NOT fix them. The FarmDashboardPage clickable-card keyboard-a11y item is owned by 5.10e3 (NOT 5.10e2). The `bg-info text-white` contrast item is owned by a post-MVP theming pass (NOT 5.10e2).
- `docs/implementation-artifacts/epic-5-context.md` — compiled epic context (reuse if valid; see step-01 rules).
- `docs/architecture.md` — frontend component structure, composable conventions, store patterns. Read for the established Pinia store conventions before adding caching.
- `docs/PRD.md` — NFR-9 (Performance: <3 seconds on 3G for key pages), NFR-5 (mobile 320px+ — boundary reference, not 5.10e2's concern).
- `docs/ux-specification.md` — §"Loading States" / §"Animation & Motion" (loading patterns — 5.10e2's caching must not break the 5.10e1 loading states).

Key design decisions for the spec to resolve:

- **Caching invalidation strategy:** the spec MUST define how caches are busted after CUD actions. Options: (a) each CUD action calls `store.invalidate()` which clears `loaded = false` and the next page mount re-fetches; (b) each CUD action appends/updates the cached array in-place (optimistic update). Recommend (a) for simplicity and correctness (matches the `farmInputsLoaded = false` pattern in `inventory-store.js`); (b) is more performant but error-prone (pagination, sorting, computed fields). The spec MUST enumerate the exact invalidation call sites per store.
- **Caching scope:** which stores to cache and which to exclude. MUST cache: high-traffic list-page stores (households, residents, inventory, finance categories/funding, farm plots/crops, school classes/learners, vendors). MUST NOT cache: notifications (freshness for unread badge), auth (session-critical), settings (may change via admin). The spec MUST enumerate the exact stores.
- **Caching TTL:** should the cache be permanent (until invalidated by a CUD action) or time-based (e.g., re-fetch if >5 minutes old)? Recommend permanent-until-invalidated for MVP simplicity (matches existing `loaded` boolean pattern); time-based TTL adds complexity and a `lastFetch` timestamp. Document the decision in the spec.
- **Shared caching helper:** should the spec extract a `useStoreCache()` composable, or add caching in-place to each store? Recommend in-place unless >6 stores share the IDENTICAL caching pattern (loaded boolean + invalidate method + skip-in-fetch). The spec MUST grep `src/composables/` for existing caching helpers first.
- **3G test threshold:** AC5 says "<3 seconds on 3G." The spec MUST define what "on 3G" means (Chrome DevTools "Slow 3G" preset: ~400KB/s download, ~400ms RTT) and which pages are "key pages" (recommend: Dashboard, Households list, Residents list, Finance transactions, Farm dashboard, School dashboard — the highest-traffic surfaces). If a page exceeds 3s, the spec MUST propose a targeted fix or defer with rationale.
- **Bundle size threshold:** what's a reasonable per-chunk size threshold? Recommend flagging any chunk >250KB that loads on a common navigation path (not on-demand export). The jspdf (390KB) and html2canvas (195KB) chunks are on-demand (PDF export) — acceptable. The chart.js chunk (198KB) should be on-demand after 5a's dynamic conversion — verify.
- **No new dependencies:** all fixes use existing patterns. No new npm packages (no bundle analyzers like `rollup-plugin-visualizer` unless already in `package.json` — if a bundle analyzer is needed, HALT with `new dependency required: <name> — user approval needed`). Use `npm run build` output + manual `dist/spa/assets/*.js` size inspection for the bundle audit.
- **i18n / emojis:** none (hardcoded English, no emojis) — consistent with project convention.

## Planning Artifacts to Load

Authoritative sources (load via compile-epic-context subagent for epic-5-context.md if not already compiled, plus selectively for story-specific constraints):

- docs/epics.md — Story 5.10 ACs (AC5 is the focus for 5.10e2; AC4 was 5.10e1 DONE; AC6/AC9 are 5.10e3/e4; AC8 deferred to post-MVP) and Epic 5 story list
- docs/PRD.md — NFR-9 (Performance: <3 seconds on 3G for key pages), NFR-5 (mobile 320px+ — this is 5.10e3's concern, referenced for boundary clarity), UX Design Principles (consistency, intuitive workflows)
- docs/architecture.md — frontend component structure, Pinia store conventions, composable conventions, `() => import()` route-lazy-loading pattern
- docs/ux-specification.md — §"Loading States" / §8 "Animation & Motion" (loading patterns — 5.10e2's caching must not break the 5.10e1 loading states)
- docs/implementation-artifacts/spec-5-10e1-ux-polish-and-accessibility.md — 5.10e1 spec (DONE); read to confirm 5.10e1's loading-state and error-handling fixes are in place so 5.10e2's caching changes don't conflict. NOTE the post-finalization patches `26779c6` (page-header standardization) and `6f251e7` (page margins).
- docs/implementation-artifacts/spec-5-10a-dashboard-completion-real-data-wiring.md — 5.10a spec (DONE); 5.10e2 removes the 300ms setTimeout in `useDashboardData.js` that 5.10a introduced as a hydration-safety workaround.
- docs/implementation-artifacts/spec-5-10b-navigation-polish-breadcrumbs-and-quick-search.md — 5.10b spec (DONE); 5.10e2 must not break quick-search/breadcrumbs. NOTE the post-finalization patch `3152db1`.
- docs/implementation-artifacts/spec-5-10c-notifications-system.md — 5.10c spec (DONE); 5.10e2's caching audit MUST exclude `notifications-store` from caching (freshness for unread badge). NOTE the post-review patch `f5e5fc9`.
- docs/implementation-artifacts/spec-5-10d-help-and-documentation.md — 5.10d spec (DONE); 5.10e2 must not break help icon/menu/tooltips. NOTE the post-review patch `4abd369`.
- docs/implementation-artifacts/epic-5-context.md — compiled epic context (reuse if valid; see step-01 rules)
- docs/implementation-artifacts/deferred-work.md — carry-forward items; the 300ms setTimeout is owned by 5.10e2 (this story). The 3 security/realtime items (notification_reads griefing, realtime async-failure, at-risk cache re-flip) are re-deferred to post-MVP per user decision 2026-08-11 — 5.10e2 does NOT fix them. The FarmDashboardPage clickable-card keyboard-a11y item is owned by 5.10e3 (NOT 5.10e2). The `bg-info text-white` contrast item is owned by a post-MVP theming pass (NOT 5.10e2).
- docs/planning-artifacts/sprint-change-proposal-2026-07-28.md — confirms 5.10's dependency on all prior MVP stories

Do NOT load POST-MVP.md as a primary source — it lists deferred modules only. Use it only to confirm a feature is deferred when in doubt.

## Project Conventions (non-negotiable)

- Frontend: Quasar v2.18.5 (Vue 3 + Vite + SSR), `<script setup>` syntax mandatory.
- Backend: Appwrite v21.2.1 (Database, Auth, Storage, Functions).
- State: Pinia. Date/Time: date-fns + date-fns-tz (village timezone from `settingsStore.timezone`, default `Africa/Lusaka`). Charts: Chart.js v4.5.1. Calendar: vue-cal v5 (`^5.0.1-rc.33`).
- Normalized ID-based relationships; composable error handling (useErrorHandler); custom form validation integrated with error handler.
- RBAC: `src/utils/permissions.js`, `src/composables/usePermissions.js` (`hasPermission('<module>:read')`, `hasPermission('<module>:write')`), route guards, PermissionGuard — reuse, do not reinvent. 5.10e2 adds NO new permission keys and changes NO route guards (it is a performance pass on existing pages/stores/composables).
- Dashboard widgets: follow docs/implementation-artifacts/dashboard-widget-pattern.md exactly.
- No new dependencies without verifying they're already in package.json. If a new dep is truly required, HALT with blocking condition `new dependency required: <name> — user approval needed`.
- Match existing code style in src/pages/, src/stores/, src/composables/, src/services/, src/modules/. Read neighboring modules (e.g. src/modules/school/, src/modules/farm/, src/modules/calendar/, src/modules/storage/, src/pages/setup/, src/pages/admin/) before scaffolding.
- i18n: NOT implemented in this project. vue-i18n is NOT installed. All UI strings are hardcoded English, matching existing modules (Epics 1–4 and Stories 5.1–5.14). This is a user-approved decision (2026-07-28); i18n is deferred to post-MVP — see docs/implementation-artifacts/deferred-work.md. Do NOT add vue-i18n. Do NOT use $t() or useI18n(). Write hardcoded English strings consistent with existing modules.
- No emojis in code or UI unless an existing module already uses them (do not remove existing ones).

## Strict Readiness Standard (HALT on any violation)

Before leaving step-02 (plan), the spec MUST satisfy the "Ready for Development" standard from SKILL.md:

- Actionable: every task has a file path and specific action.
- Logical: tasks ordered by dependency.
- Testable: all ACs in Given/When/Then form.
- Complete: no placeholders or TBDs.
- Sufficient: no unresolved requirement/acceptance/dependency/implementation gaps.
- Coherent: no internal contradictions.

HALT with status `blocked` and a precise blocking condition if ANY of these occur:

- A story AC cannot be translated into concrete tasks because a requirement is ambiguous or contradictory between epics.md / PRD.md / ux-specification.md / architecture.md.
- A prerequisite story is not actually `done` (verify against docs/sprint-status.yaml, not against docs/bmm-workflow-status.md which is stale).
- The working tree is dirty or the current branch is an obvious mismatch for Epic 5 work.
- A new third-party dependency is required.
- An AC requires functionality that belongs to a deferred story (5.5/5.6/5.8) or a not-yet-built story — split the work cleanly and exclude the forward-dep portion; if the AC cannot be satisfied without it, HALT.
- The intent resolves to multiple independently shippable goals that cannot be scoped into one spec — set `warnings: [multiple-goals]` in frontmatter and proceed only if they're genuinely inseparable; otherwise HALT. (5.10e2 is AC5 — lazy-loading audit + bundle size check + 300ms setTimeout removal + caching audit + 3G test documentation. Each sub-AC (5a-5e) caps its fix set. If the spec derivation finds the total fix count still exceeds one iteration, HALT with a proposal to split 5.10e2 into 5.10e2a (lazy-loading + bundle size + setTimeout — 5a-5c) and 5.10e2b (caching + 3G test — 5d-5e) — do NOT silently expand scope. The broader 5.10 was split into 5.10a–5.10e per user decision 2026-08-04, and 5.10e was split into 5.10e1–5.10e4 per user decision 2026-08-11.)

Do NOT invent requirements. Do NOT pull scope from other stories into this iteration. Do NOT implement features that belong to a later story in the dependency order.

## Implementation Discipline (step-03)

- Follow the spec's task list in order. Do not reorder.
- Reuse existing composables, stores, services, and RBAC utilities. Do not duplicate. (5.10e2 reuses the existing `() => import()` route-lazy-loading pattern, the existing `await import()` dynamic-import pattern for chart.js/jspdf, the existing `loaded`/`isLoading` ref patterns in `inventory-store.js`/`calendar-store.js`, and the existing Pinia store structure.)
- Vue 3 `<script setup>` only. No Options API. No `this`.
- SSR-safe: 5.10e2 modifies existing composables (`useDashboardData.js`), stores (caching guards), and component import statements (static → dynamic) — all client-safe. No new `tables.listRows`, realtime, or Functions calls are introduced. The 300ms `setTimeout` removal in `useDashboardData.js` must not cause a hydration mismatch (the fetches are already in `onMounted`, which only runs client-side). Dynamic import conversions (`await import('chart.js')`) must be wrapped in `onMounted` or event handlers (not at module scope) to stay SSR-safe. Any new composable (e.g., `useStoreCache.js` IF justified) must be SSR-safe (no Appwrite calls, no `window`/`document` access at module scope).
- Quasar components for all UI primitives. No raw HTML controls. (5.10e2 changes import strategies and store fetch logic — no new UI primitives are introduced.)
- 5.10e2 is REUSE-ONLY: it adds NO new Appwrite infrastructure (no tables, no server functions, no permissions, no `.env` vars, no stores, no pages, no routes). The frontend artifacts modified are a bounded set of existing composables, stores, and component import statements (the spec enumerates exact files per sub-AC 5a-5e). OPTIONALLY a shared `useStoreCache.js` composable may be added IF the spec justifies extraction (per 5d design decision). The spec MUST include tasks for all modifications.
- Pinia stores: 5.10e2 creates NO new stores but MODIFIES existing stores (adds `loaded`/`invalidate` caching guards to high-traffic stores per 5d). The spec MUST enumerate which stores are modified and verify no stale-data bugs are introduced (every CUD action busts the relevant cache).
- Date handling: N/A for 5.10e2 (no date logic changes — this is a performance pass).
- Permission checks: 5.10e2 adds NO new permission keys and changes NO route guards. It does NOT alter which users can see which pages — only how fast those pages load (lazy-loading, caching, delay removal).
- No emojis in code or UI unless an existing module already uses them (do not remove existing ones).

## Review (step-04) — Full Adversarial

Run the full adversarial review pass per the skill's step-04:

- Blind Hunter: does the implementation actually satisfy each AC as written, with no hidden gaps?
- Edge Case Hunter: walk every branching path and boundary.
- Acceptance Auditor: map each AC to concrete code/test evidence; flag any AC with no evidence.

### Review invariants for Story 5.10e2 <<< CHANGE PER ITERATION >>>

Specific invariants the review MUST verify for 5.10e2:

- **Lazy-loading (5a):** all 24 route components in `src/router/routes.js` still use `() => import(...)` (no static route imports introduced); the 2 known static chart.js imports (`FinanceReportsPage.vue:755`, `InventoryStockWidget.vue:111`) are converted to dynamic `await import('chart.js')` matching the established pattern in `FarmReportsPage.vue`/`PlotDetailPage.vue`/etc.; no other static heavy-vendor-lib imports exist (grep-verified); the dynamic conversions are SSR-safe (wrapped in `onMounted` or event handlers, not at module scope); no page's functionality is broken by the import change (charts still render, PDF export still works).
- **Bundle size (5b):** the spec documents the current top 10-15 chunk sizes from `npm run build` output; the 5a dynamic conversions are verified to reduce or relocate the chart.js chunk (it should no longer be in the FinanceReportsPage or dashboard chunks — it becomes a shared on-demand chunk); any chunk >250KB that loads on a common navigation path is flagged (on-demand export chunks like jspdf/html2canvas are acceptable); if a chunk cannot be reduced without a new dependency or architectural change, it's documented as a deferred item with rationale.
- **300ms setTimeout removal (5c):** the `await new Promise((resolve) => setTimeout(resolve, 300))` in `useDashboardData.js` is removed; the dashboard still hydrates correctly (no hydration mismatch, no race condition — verify by loading the dashboard with real data after the change); the dashboard's 5.10e1 loading state still works (shows loading indicator during fetch, clears on completion); the dashboard's 5.10a isolated-fetch error handling still works (one module failure doesn't break others).
- **Caching (5d):** each enumerated store (per spec's 5d fix list) now has a `loaded` boolean (or equivalent) caching guard that skips re-fetch on subsequent page mounts; every CUD action in that store busts the cache (calls `invalidate()` or sets `loaded = false`) so the next mount re-fetches; no stale-data bug exists (after creating/editing/deleting an item, the list shows the updated data — verify by tracing each CUD action's cache-bust call); `notifications-store` is NOT cached (excluded for freshness); `auth-store` and `settings-store` are NOT cached (session-critical); the first load still shows a loading state (5.10e1's loading states are not bypassed by the cache-skip — the loading state shows on first fetch, then the cached data renders instantly on subsequent loads); if a shared `useStoreCache.js` composable was extracted, it's reused consistently and is SSR-safe.
- **3G test documentation (5e):** the spec documents the 3G throttle test procedure (Chrome DevTools "Slow 3G" preset) and records results for the key pages (Dashboard, Households list, Residents list, Finance transactions, Farm dashboard, School dashboard); any page exceeding 3s is flagged with a targeted optimization proposal or a deferral with rationale; the results are in the spec (and optionally in `docs/implementation-artifacts/performance-test-results.md`).
- **No new Appwrite infrastructure:** NO new tables, server functions, permissions, `.env` vars, stores, pages, or routes were added. Verify by checking `setup-appwrite.js`, `seed-roles.js`, `permissions.js`, `.env.example`, `src/stores/` (no NEW store files — existing stores are modified per 5d), `src/router/routes.js`, and `src/pages/` for any 5.10e2-related additions (there should be NONE, except OPTIONALLY a shared `useStoreCache.js` in `src/composables/` IF the spec justified extraction, and OPTIONALLY a `performance-test-results.md` doc).
- **No backend changes:** no server functions modified, no `appwrite.config.json` changes, no seed data changes.
- The 5.10b quick-search box, the 5.10c notifications bell + badge + panel + realtime subscription (NOT cached — freshness preserved), the 5.10d help icon + menu + 20 tooltips, the 5.10e1 loading states + error handling + a11y fixes (loading states still show on first fetch, not bypassed by caching), the `expandedSections` auto-expand watcher, breadcrumbs, the 5.10a dashboard real-data wiring (minus the removed 300ms delay), the 5.11 empty-state banners, the 5.9 module management, the 5.12/5.13 user/role management, the 5.14 password flows, and all prior module dashboards/pages are NOT broken — 5.10e2 only changes import strategies, removes the artificial delay, and adds caching guards; it does not change any page's data flow or permissions.
- NO 44px touch-target sizing changes appear in 5.10e2 (that's AC6 / 5.10e3). NO accessibility/aria-label/contrast changes appear in 5.10e2 (that's AC4 / 5.10e1, DONE). NO testing-checklist doc is produced in 5.10e2 (that's 5.10e4). NO security/realtime fixes (notification_reads griefing, realtime async-failure, at-risk cache re-flip) appear in 5.10e2 (re-deferred to post-MVP per user decision 2026-08-11). NO FarmDashboardPage clickable-card keyboard-a11y fix appears in 5.10e2 (that's 5.10e3 per user decision 2026-08-11). NO `bg-info text-white` contrast fix appears in 5.10e2 (post-MVP theming pass).
- If the spec set `warnings: [multiple-goals]` and HALTed to propose splitting 5.10e2 into 5.10e2a/5.10e2b, the review verifies the HALT was clean and no partial implementation was left.

**5.10 sub-story roadmap (for context — do NOT implement future sub-stories in this iteration):**

- **5.10a (DONE 2026-08-04):** Dashboard Completion — Real Data Wiring (AC1). Replaced placeholder data, verified widgets functional, <2s load.
- **5.10b (DONE 2026-08-05):** Navigation Polish — Breadcrumbs and Quick Search (AC2). Added breadcrumbs to detail/form/create/edit pages (Module → List → Detail, responsive), global header quick search with grouped results dropdown, fixed active highlighting (auto-expand nav section for active child route), clean-menu audit (removed dead `/communications` link). Post-finalization patch `3152db1` corrected a defective learner search (learners table has no name columns — reworked to search residents then resolve learner rows), the finance result field, and a header-search focus issue.
- **5.10c (DONE 2026-08-05):** Notifications System (AC3). New Appwrite `notifications` + `notification_reads` tables, `createNotification` server function (role-targeted delivery), notifications store, bell icon + count badge in MainLayout header, notification panel with filter by type and mark-as-read, Appwrite realtime live updates, and a bounded set of REAL role-targeted triggers (at-risk learner newly flagged → school roles; farm alert newly raised → `farm:read`; new vendor created → finance/farm roles). In-app only (email deferred). Post-review patch `f5e5fc9` fixed single-role users in `Query.or`. This was the only 5.10 sub-story that added new Appwrite infrastructure.
- **5.10d (DONE 2026-08-10):** Help and Documentation (AC7). Help icon in header (between bell and user profile), responsive help menu (q-menu desktop / q-dialog mobile) with User Guide / FAQ / About, new `/help` page with User Guide tab (12 sections, module-enabled-aware for Farm/School/Vendors) and FAQ tab (17 Q&A across 5 categories), and exactly 20 enumerated `q-tooltip` elements across MainLayout.vue and 9 other existing pages. Reuse-only — no new Appwrite infrastructure. Post-review patch `4abd369` fixed the help button header placement. Review Triage Log §2026-08-10 is authoritative for the current code state.
- **5.10e (split into 5.10e1–5.10e4 per user decision 2026-08-11):** UX Polish, Performance, Mobile Responsiveness, and Final Testing Checklist (AC4, AC5, AC6, AC9):
  - **5.10e1 (DONE 2026-08-11):** UX Polish and Accessibility (AC4). Consistent UI (3 heading-hierarchy fixes + post-finalization patch `26779c6` standardizing page headers across 65 pages + `6f251e7` page margins), 9 loading-state additions, 8 error-handling standardizations, 10 success-notification standardizations, 17 accessibility fixes (icon-only aria-labels, notification-badge live region, bounded `bg-warning text-dark` contrast fix), and the 2 deferred 5.10b a11y fixes (Breadcrumbs back-button destination-specific aria-label, quick-search keyboard nav via `highlightedIndex`/`@keydown` preserving `no-focus no-refocus`). Reuse-only. 1 deferred item (FarmDashboardPage clickable-card keyboard a11y → 5.10e3 per user decision 2026-08-11). `bg-info text-white` contrast deferred to post-MVP theming pass. Review Triage Log §2026-08-11 is authoritative for the current code state; `final_revision: 6f251e7`.
  - **5.10e2 (THIS ITERATION):** Performance Optimization (AC5). Lazy-loading audit (2 static chart.js imports → dynamic), bundle size check, caching (bounded `loaded`/`invalidate` guards on high-traffic stores), remove the 300ms `setTimeout` in `useDashboardData.js`, document the 3G manual throttle test. Reuse-only.
  - **5.10e3:** Mobile Responsiveness (AC6). Full 320px audit of ALL pages + all fixes, 44px touch-target audit (per user decision 2026-08-11: full audit + all fixes), FarmDashboardPage clickable-card keyboard a11y (per user decision 2026-08-11: folded into 5.10e3). Reuse-only.
  - **5.10e4:** Final Testing Checklist (AC9). Manual final-testing-checklist.md doc covering all MVP user journeys, RBAC matrix, data integrity, integrations, sample-data mode (per user decision 2026-08-11: manual checklist only; automated test infrastructure deferred to a new post-MVP testing epic — 5.10e4 documents the new epic in deferred-work.md). This is the FINAL 5.10 sub-story and the FINAL Epic 5 story — completing it marks 5.10 and Epic 5 as done.
- **AC8 (System Health Monitoring): DEFERRED to post-MVP** (user decision 2026-08-04). Documented in `deferred-work.md` during 5.10a; 5.10e4 will verify/expand the deferral entry (proposed admin page layout, server function requirements, data sources, estimated post-MVP effort). Not implemented in MVP.

**Prior Epic 5 story summaries (for regression-checking context):**

**5.9 (Module Management):** Admin page at `/admin/modules`. Core modules always enabled (Residents, Households, Finance, Inventory, Calendar, Storage). Optional MVP modules toggleable: Farm, School, Vendors ONLY (NOT Guests/Equipment/Energy — deferred). Toggle hides nav/widgets but preserves data. Dependency warning on disable. Updates `settingsStore.modulesEnabled`. Dep: all MVP previous stories.

**5.14 (Auth Completeness):** ProfilePage "Change Password" dialog. AuthPage "Forgot password?" link → `Account.createRecovery` → email link → `/auth/reset-password` page → `Account.updateRecovery`. Email verification deferred. No self-service signup. Deps: 1.3, 1.11.

**5.12 (User CRUD):** UsersPage `/admin/users` "Add User" button (System Admin only). Server-side Appwrite Function for admin-scope user creation. Soft-deactivate. Cannot deactivate self or last System Administrator. Audit logging. Deps: 1.4, 1.11.

**5.13 (Role Assignment UI):** UsersPage "Manage Roles" dialog. "View Permissions" shows effective permission union. `/admin/roles` page: role list with permission matrix. Read-only for MVP. `seed-roles.js` upsert-capable. Dep: 5.12.

**5.11 (Start Fresh Wizard):** SetupWizard "Start Fresh" card enabled. 5-step wizard: Village Profile → Admin User → Village Head → Module Selection → First Household. Sets `is_using_sample_data = false`. Empty-state CTAs on dashboard and list pages. Last-System-Admin guard added to `updateUser`. Deps: 5.9, 5.12. DONE 2026-08-04.

**5.10a (Dashboard Completion — Real Data Wiring):** Replaced `DashboardPage.vue` placeholder data with real, permission-gated Appwrite fetches via new `src/composables/useDashboardData.js` (household/resident counts via `Query.limit(1)` + `.total`; finance totals via `financeStore.fetchSummary()`; recent activity via per-module `tables.listRows` with `Query.orderDesc('$createdAt')` + `Query.limit(3)`, merged/sorted/sliced to 8). Extended `QuickStatsWidget` with Total Income/Total Expense cards. Each module fetch isolated (try/catch) so one failure doesn't break the dashboard. SSR-safe (onMounted only). AC8 (System Health Monitoring) deferred to post-MVP and documented in `deferred-work.md`. NOTE: a 300ms `setTimeout` hydration delay remains in `useDashboardData.load()` — owned by 5.10e2 (performance) for removal. Dep: all prior MVP stories. DONE 2026-08-04.

**5.10b (Navigation Polish — Breadcrumbs and Quick Search):** Added reusable `src/components/layout/Breadcrumbs.vue` (responsive Module → List → Detail, mobile back-button-only) replacing ad-hoc Back buttons on 22 detail/form/create/edit pages; added `meta.breadcrumb` to existing routes; added `src/composables/useGlobalSearch.js` (permission-gated, SSR-safe quick search across households/residents/finance/plots/learners/vendors/inventory/calendar + nav items, grouped dropdown); added a route watcher in `MainLayout.vue` to auto-expand the active `expandedSections` key; removed the dead `/communications` nav link. Post-finalization patch `3152db1` (2026-08-05) corrected a defective learner search (the `learners` table has NO `first_name`/`last_name` columns — it joins `residents` via `resident_id`; reworked to search residents first then resolve learner rows), the finance result secondary field (`amount_funded ?? amount` + ZMW currency + date), and a header-search focus issue (`no-focus no-refocus` on the results `q-menu`). The 5.10b spec's `final_revision` is `3152db1`; its Review Triage Log §2026-08-05 is authoritative for the current code state. Dep: 5.10a. DONE 2026-08-05.

**5.10c (Notifications System):** Added new Appwrite `notifications` table (role-targeted, one row per event) and `notification_reads` table (per-user read receipts, row-secured matching the `file_metadata` precedent). New `createNotification` Appwrite Function derives `target_roles` from a hardcoded `TYPE_CONFIG` authorization+targeting matrix (never trusts client-supplied targeting; validates caller permission per type). New `src/stores/notifications-store.js` Pinia store (fetchMyNotifications with role-intersection query + read-receipt join, unreadCount getter, markRead/markAllRead, filterByType, createNotification helper). Bell `q-btn` + `q-badge` + `NotificationPanel.vue` in `MainLayout.vue` header (q-menu desktop, q-dialog mobile). Appwrite realtime subscription with 30s polling fallback. Exactly 3 triggers: at-risk learner newly flagged (school roles, dedup by type+related_entity_type+related_entity_id), farm alert newly raised (farm roles, same dedup), new vendor created (finance/farm roles). Seeded demo notifications in `seedAllData`. Review: 2 patches (1 high — deterministic SHA-256 rowId for race-free dedup; 1 low — missing `delete` permission on `notification_reads`), 3 deferred items (table-level create griefing vector, realtime sync-only error handling, at-risk 60s cache re-flip suppression — all pre-existing-pattern-class; per user decision 2026-08-11 these 3 security/realtime items are re-deferred to post-MVP hardening, NOT owned by any 5.10e sub-story), 7 rejected findings. Post-review patch `f5e5fc9` fixed single-role users in `Query.or` (used `Query.contains` for single role, `Query.or`+`Query.contains` for multiple). Dep: 5.10b. DONE 2026-08-05.

**5.10d (Help and Documentation):** Added a help `q-btn` (`icon="help"`) in `MainLayout.vue` header between the 5.10c notifications bell and the user profile `q-btn`, with a responsive help menu (`q-menu` desktop / `q-dialog` mobile) containing User Guide / FAQ / About (version + village name) links — shared via a new `src/components/layout/HelpMenuList.vue` component (extracted during review patch to deduplicate the menu/dialog markup, mirroring the `NotificationPanel.vue` reuse pattern). Added a new `/help` route (`requiresAuth: true`, no `requiresPermission`, no breadcrumb — top-level page like Dashboard) and a new `src/pages/help/HelpPage.vue` with two `q-tab-panels`: "User Guide" (Getting Started card + 11 `q-expansion-item` module sections, module-enabled-aware for Farm/School/Vendors showing a "not enabled" banner post-hydration) and "FAQ" (17 Q&A across 5 categories: General, Data Entry, Roles & Permissions, Troubleshooting, Sample Data vs Real Data). Added exactly 20 enumerated `q-tooltip` elements: help icon, notifications bell, quick-search input, 7 nav-section headers in `MainLayout.vue`, 7 primary action buttons across 6 list pages, and 3 complex form fields (VillageSettingsPage currency, StorageSettingsPage quota, LongTermGoalsSettingsPage benchmark threshold — substituted for the non-existent at-risk attendance threshold UI control). Reuse-only — no new Appwrite tables, functions, permissions, `.env` vars, or stores. Review: 1 patch (low — `HelpMenuList.vue` extraction), 16 rejected findings (all out-of-scope or factually incorrect), 0 deferred. Post-review patch `4abd369` fixed the help button header placement. The 5.10d spec's `baseline_revision` is `d6e8be0`; its Review Triage Log §2026-08-10 is authoritative for the current code state. Dep: 5.10c. DONE 2026-08-10.

**5.10e1 (UX Polish and Accessibility):** Audit-and-fix pass delivering AC4. 4a Consistent UI: 3 heading-hierarchy fixes (HouseholdsListPage/FinanceReportsPage → `text-h5`, DashboardPage `text-h4` → `text-h5`); post-finalization patch `26779c6` extended this to ALL 65 pages (standardized `<h4 class="text-h5 q-my-none">` + `<p class="text-grey-7 q-mb-none">` subtitle), plus `6f251e7` page-margin alignment on 9 pages. 4b Loading states: 9 pages got loading indicators (InventoryListPage, FarmDashboardPage, CropsListPage, PlantingsListPage, SalesListPage, ClassesListPage, VendorsListPage, AllLoansPage; CalendarPage already covered). 4c Error handling: 8 standardizations (DashboardPage + 10 useDashboardData catch blocks got non-blocking `notifyError`; farm-store 6 catch blocks got `notifyError`; CreatePlantingPage error → `handleError`; VillageSettingsPage 5 catches → `notifyError`; MainLayout realtime-subscribe-failure notified once; lendingStore + notifications-store de-duplicated redundant `console.error`). 4d Success confirmations: 10 CUD actions standardized to `useErrorHandler().notifySuccess()` (PlotFormPage create/update, PlotsListPage delete, CropFormPage create, PlantingDetailPage harvest start/complete, CreatePlantingPage record, InventoryFormPage create/update, InventoryDetailPage sale, MainLayout logout, FinanceDashboardPage export, FinanceTransactionsPage funding add, VillageSettingsPage council member save/remove). 4e Accessibility: 17 fixes (5 icon-only aria-labels each on HouseholdsListPage + ResidentsListPage, 3 on PlotsListPage, 2 on LearnersListPage, 1 on LongTermGoalsSettingsPage back button, 1 on FinanceTransactionsPage inventory-link, 3 refresh-button aria-labels on farm widgets, notification-badge `role="status"` + `aria-live="polite"`, 6 `bg-warning text-white` → `text-dark` contrast fixes). 4f Deferred a11y: Breadcrumbs.vue `backAriaLabel` computed (destination-specific, derived from `props.items`); MainLayout quick-search keyboard nav via `highlightedIndex` ref + `flattenedResults` computed + `@keydown` handler (ArrowDown/ArrowUp/Enter/Escape) preserving `no-focus no-refocus`. Reuse-only — no new Appwrite infra. Review: 1 patch (medium — malformed `deferred-work.md` entry corrected), 1 deferred (medium — FarmDashboardPage clickable-card keyboard a11y → 5.10e3 per user decision 2026-08-11), 19 rejected. `bg-info text-white` contrast (~1.8:1, 10+ files) deferred to post-MVP theming pass. The 5.10e1 spec's `baseline_revision` is `2acced5`; `final_revision` is `6f251e7`; its Review Triage Log §2026-08-11 is authoritative for the current code state. Dep: 5.10d. DONE 2026-08-11.
